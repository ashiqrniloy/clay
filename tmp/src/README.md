# LSP Manual Test

Ordinary prose should use the normal editor text color.

## Rust

The [Rust source](main.rs) defines `Point` and `distance_from_origin`.

## TypeScript

The [TypeScript source](main.ts) defines a `User` interface and `greet` function.

## JavaScript

The [JavaScript source](main.js) defines an `add` function.

- Lists, links, headings, and code should remain visibly decorated.
- [[missing-page]] should produce a Marksman diagnostic.
