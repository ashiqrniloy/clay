{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Main webview: core defaults only. No filesystem, shell, process, or network plugin permissions; the Clay server process is supervised by desktop Rust code, never by webview commands.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
