# Phase 18.10: Package-Provided Tree-sitter Grammar and Syntax Highlighting Primitive

## Objectives
- Add a generic `SyntaxGrammarContribution` primitive so packages can provide Tree-sitter grammar artifacts, highlight queries, style-token mappings, provenance, and performance metadata.
- Create first-party grammar-only packages `@clay/rust`, `@clay/typescript`, and `@clay/javascript` without full language modes, language-specific commands, completion providers, or bespoke Rust branches.
- Parse and highlight Rust, TypeScript, and JavaScript through server-side background work that publishes bounded `DecorationSet` values to the existing client decoration renderer.
- Keep active syntax grammar separate from active major mode so grammar packages can attach highlighting to `core.code`/`core.text` fallback documents from Phase 18.9.
- Preserve hot-path, authority, and package-security invariants: no package JavaScript in Masonry paint/text-event handlers, no client-side parser execution, no arbitrary native/third-party grammar artifact loading, no filesystem/network/shell/AI/WASM/raw-op/native-widget/package-manager authority expansion.

## Expected Outcome
- Opening Rust, TypeScript, and JavaScript files can show syntax highlighting sourced from explicitly loaded first-party grammar packages while documents still use generic fallback modes unless a full mode package later takes ownership.
- Clay core owns the grammar contribution contract, Tree-sitter execution boundary, validation, parse scheduling, decoration transport, and renderer safety; packages own grammar/query assets and language metadata.
- Syntax parse/highlight work is asynchronous, cancellable, stale-version-rejecting, viewport-prioritized, and bounded by existing parse/decorator budgets.
- Tests and docs prove grammar resolution, disabled/invalid package fallback, query diagnostics, decoration payload limits, and absence of language-specific Rust server/client branches.

## Entry Gate

- Do not start until Phase 18.9 generic `core.text`/`core.code` fallback modes and deterministic document classification fallbacks are complete (`plans/037-Phase18.9-Generic-Text-Code-Modes-Key-Behavior-and-Mode-Discovery-Fallbacks.md`).
- Treat arbitrary third-party grammar/native artifact loading as out of scope. This phase may load only resolver-validated first-party package grammar artifacts. If implementation needs broader native, third-party, package-manager, filesystem, or WASM authority, stop and request an approved decision log before coding that expansion.
- Use Tree-sitter docs and local Rust crate docs before coding against Tree-sitter APIs. Initial documentation lookup used:
  - `npx ctx7@latest library "Tree-sitter" "Create plan document for Phase 18.10: Package-Provided Tree-sitter Grammar and Syntax Highlighting Primitive from roadmap.md"` -> selected `/tree-sitter/tree-sitter` and `/websites/rs_tree-sitter`.
  - `npx ctx7@latest docs /tree-sitter/tree-sitter "... incremental parsing, grammar artifacts, highlight queries, query captures, Rust package integration, security constraints."`
  - `npx ctx7@latest docs /websites/rs_tree-sitter "... Rust API for Parser, Language, Query, QueryCursor, incremental parsing, grammar loading, and query captures."`

## Tasks

- [x] Review existing editor primitives and plan generic grammar gaps before language package work
  - Acceptance Criteria:
    - Functional: Existing document classification, major-mode activation, package loading, parse coordinator, decoration transport, style-token validation, package manifests, package permissions, docs registry, and wiki coverage are inventoried before new Rust code is written.
    - Performance: Review separates package load/grammar validation from background parse/highlight work and from paint/text-event hot paths; no synchronous parse/highlight before local paint.
    - Code Quality: New primitive gaps are named generically (`SyntaxGrammarContribution`, grammar registry, query/capture validation, syntax provider selection), not Rust/TypeScript/JavaScript-specific branches.
    - Security: Review records first-party-only grammar artifact scope, package-root path confinement, no arbitrary native/third-party artifact loading, and no new filesystem/network/shell/AI/WASM/raw-op/native-widget/client-JS/package-manager authority.
  - Approach:
    - Documentation Reviewed:
      - `roadmap.md`: Phase 18.10 scope and Phase 18.9 entry gate.
      - `docs/reference/primitives/index.md`, `registry.md`, `backlog.md`, `parse-update-strategy.md`, `rendering-strategy.md`, `package-security.md`.
      - `docs/wiki/modules/parse-coordinator.md`, `decoration-transport.md`, `mode-registry.md`, `package-loading.md`, `phase18.9-generic-text-code-modes-primitive-review.md`.
      - `.agents/skills/project-patterns/references/mode-primitive-first.md`, `language-capability-sequencing.md`, `package-distribution.md`, `protocol-and-performance.md`, `authority-boundaries.md`, `documentation-as-code.md`.
      - Context7 Tree-sitter docs listed in the Entry Gate.
    - Options Considered:
      - Add Rust/TypeScript/JavaScript grammar handling directly to Clay core: fastest, but violates package-provided grammar ownership and creates language branches.
      - Use package JavaScript parsers for highlighting: reuses package runtime, but duplicates Tree-sitter engine work and risks slow JS on edit/open paths.
      - Add a generic syntax grammar primitive and run Tree-sitter server-side from package-provided assets: preserves package ownership while keeping parser execution in a bounded Rust-owned background path.
    - Chosen Approach:
      - Inventory current primitives, then add only reusable grammar contribution/selection/validation gaps. Build first-party grammar packages on top of those gaps.
    - API Notes and Examples:
      ```text
      SyntaxGrammarContribution {
        packagePrefix, languageId, filePatterns,
        grammarArtifact, highlightsQuery, localsQuery?, injectionsQuery?,
        captureStyleMap, version, provenance, budgets
      }
      ```
    - Files to Create/Edit:
      - `docs/wiki/modules/phase18.10-tree-sitter-grammar-primitive-review.md`: primitive inventory, gaps, rejected language-specific shapes, authority/performance boundary.
      - `docs/reference/primitives/registry.md`: add `SyntaxGrammarContribution` row/category notes.
      - `docs/reference/primitives/backlog.md`: mark Phase 18.10 syntax grammar row/status.
      - `tests/primitives_docs.rs`: require primitive-review, registry, wiki index, and docs coverage.
    - References:
      - `decision-logs/2026-06-29-2006-package-provided-grammar-and-capability-phases.md`
      - `.agents/skills/project-patterns/references/language-capability-sequencing.md`
  - Test Cases to Write:
    - Primitive docs coverage: fails if `SyntaxGrammarContribution`, first-party grammar-only packages, active-syntax-vs-major-mode separation, and first-party-only artifact security are not documented in reference docs and wiki.
  - Completion Evidence:
    - Created `docs/wiki/modules/phase18.10-tree-sitter-grammar-primitive-review.md` with an inventory of document classification/major-mode activation, package loading/manifests/permissions, parse coordinator, decoration transport/style-token validation, docs registry, and wiki coverage.
    - Added the generic `SyntaxGrammarContribution` primitive gap plus grammar registry, query/capture validation, and syntax provider selection, explicitly separate from active major mode.
    - Recorded performance split: load-time grammar validation, open/reload/reclassification provider selection, background Tree-sitter parse/highlight, and no parser/package work in paint/text-event/key hot paths or before local paint.
    - Recorded security boundary: first-party-only grammar artifacts, package-root path confinement, no arbitrary native/third-party artifact loading, and no new filesystem/network/shell/AI/WASM/native-widget/raw-op/client-JS/package-manager/package-control authority.
    - Updated `docs/reference/primitives/registry.md`, `docs/reference/primitives/backlog.md`, `docs/reference/primitives/index.md`, `docs/wiki/index.md`, and `docs/wiki/modules/primitive-architecture.md` to link/record the primitive.
    - Added `tests/primitives_docs.rs::phase18_10_tree_sitter_grammar_primitive_review_records_inventory_and_gaps` coverage so the review, registry, backlog, wiki index, generic gaps, hot-path split, and authority limits stay documented.
    - Verification: `cargo fmt --check` clean; `CARGO_TARGET_DIR=target/pi-verify cargo test --test primitives_docs --quiet` passed (88 tests); `git diff --check` clean.

- [x] Verify Phase 18.9 entry gate and Tree-sitter baseline
  - Acceptance Criteria:
    - Functional: Phase 18.9 fallback modes, classification precedence, generic key behavior, and mode discovery tests pass or any blocker is recorded before implementation.
    - Performance: Existing parse/decor/no-hot-path budget tests pass before adding syntax work.
    - Code Quality: Tree-sitter dependency/API plan is based on current docs and then re-verified against locally resolved crate docs after dependency selection.
    - Security: Existing package authorization, first-party package loading, parse handler, and decoration permission tests remain green.
  - Approach:
    - Documentation Reviewed:
      - `plans/037-Phase18.9-Generic-Text-Code-Modes-Key-Behavior-and-Mode-Discovery-Fallbacks.md`.
      - Tree-sitter docs from Context7 Entry Gate.
      - `Cargo.toml`: current dependencies do not yet include Tree-sitter.
    - Options Considered:
      - Trust roadmap status: less work, but can hide stale assumptions.
      - Run focused gate checks and pin dependency docs before implementation: minimal reliable baseline.
    - Chosen Approach:
      - Run the focused gate suites first. When adding Tree-sitter crates, run `cargo metadata --format-version 1`, `cargo tree -i tree-sitter`, and `cargo doc -p tree-sitter --no-deps` with a separate `CARGO_TARGET_DIR` before using Rust APIs.
    - API Notes and Examples:
      ```bash
      CARGO_TARGET_DIR=target/pi-verify cargo test --test package_primitive_gate --quiet
      CARGO_TARGET_DIR=target/pi-verify cargo test --test parse_coordinator --quiet
      CARGO_TARGET_DIR=target/pi-verify cargo test --test decoration_transport --quiet
      CARGO_TARGET_DIR=target/pi-verify cargo test --test editor_performance_invariants --quiet
      CARGO_TARGET_DIR=target/pi-verify cargo metadata --format-version 1
      CARGO_TARGET_DIR=target/pi-docs cargo doc -p tree-sitter --no-deps
      ```
    - Files to Create/Edit:
      - `plans/038-Phase18.10-Package-Provided-Tree-sitter-Grammar-and-Syntax-Highlighting-Primitive.md`: record gate result when executing.
      - `Cargo.toml`, `Cargo.lock`: Tree-sitter dependency only after API/security shape is confirmed.
    - References:
      - `docs/wiki/modules/maintenance-validation.md`
  - Test Cases to Write:
    - Gate smoke: focused tests above pass before implementation work starts.
  - Completion Evidence:
    - Reviewed Phase 18.9 completion plan evidence in `plans/037-Phase18.9-Generic-Text-Code-Modes-Key-Behavior-and-Mode-Discovery-Fallbacks.md`, including shipped `core.text`/`core.code` fallback modes, deterministic extension/shebang/leading-content classification precedence, generic key behavior through behavior manifests, and mode discovery/listing commands.
    - Re-queried current Tree-sitter docs with ctx7: `npx ctx7@latest library "Tree-sitter" "Verify Phase 18.9 entry gate and Tree-sitter baseline for Clay Phase 18.10: current Tree-sitter docs for incremental parsing, highlight queries, Rust integration, grammar artifacts, and security constraints before adding a package-provided grammar primitive"`; selected `/tree-sitter/tree-sitter` and `/websites/rs_tree-sitter` from high-reputation matches.
    - Re-queried docs with ctx7: `npx ctx7@latest docs /tree-sitter/tree-sitter "Verify Phase 18.9 entry gate and Tree-sitter baseline for Clay Phase 18.10: current Tree-sitter docs for incremental parsing, highlight queries, grammar artifacts, Rust integration, and security constraints before adding a package-provided grammar primitive"` returned Rust setup snippets for `Parser::new`, `parser.set_language`, `tree-sitter = "0.24"`, `tree-sitter-rust = "0.23"`, incremental parsing overview, and grammar CLI note that `grammar.js` evaluation invokes Node.js.
    - Re-queried docs with ctx7: `npx ctx7@latest docs /websites/rs_tree-sitter "Verify Phase 18.9 entry gate and Tree-sitter baseline for Clay Phase 18.10: Rust Tree-sitter API baseline for Parser, Language, Query, QueryCursor, incremental parsing, grammar loading, and highlight queries before dependency selection"` returned Rust docs for `parser.set_language`, Wasm grammar loading through `WasmStore::load_language`, and core `Language`/`Tree`/`Point`/`Range`/`InputEdit` types.
    - Confirmed `Cargo.toml`, `Cargo.lock`, and `cargo metadata --format-version 1 --no-deps` currently have no `tree-sitter` dependency (`tree-sitter-absent`), so local exact crate docs and `cargo tree -i tree-sitter` remain intentionally deferred until the dependency is actually selected in a later implementation task.
    - Focused entry-gate suites passed with `CARGO_TARGET_DIR=target/pi-verify cargo test --test package_primitive_gate --test command_execution --test parse_coordinator --test decoration_transport --test editor_performance_invariants --test package_loading --test package_loading_docs --quiet` -> 145 passed across 7 suites.
    - The focused suites cover the requested gate surfaces: `tests/package_primitive_gate.rs` covers fallback mode registration/classification/package authorization, `tests/command_execution.rs` covers mode discovery/explain-active-mode behavior, `tests/parse_coordinator.rs` covers parse handler scheduling/permission/staleness, `tests/decoration_transport.rs` covers decoration permission/validation/transport, `tests/editor_performance_invariants.rs` covers no-hot-path/performance invariants, and `tests/package_loading*.rs` covers first-party/default package loading docs and behavior.
    - `cargo fmt --check` remained clean from Task 1, and `git diff --check` remained clean after this task's plan-only update.

- [x] Implement `SyntaxGrammarContribution` package metadata, validation, and registry state
  - Acceptance Criteria:
    - Functional: Packages can declare grammar-only contributions with package prefix, language ID, supported patterns, grammar artifact path/source metadata, highlight query path, optional locals/injections query paths, capture-to-style-token mapping, version/provenance, and budget metadata.
    - Performance: Load-time validation is bounded by existing package metadata limits or a new documented grammar-metadata limit; validation does not run from keypress, paint, layout, scroll, or text-event handlers.
    - Code Quality: Contribution validation is generic and package-prefix/provenance-aware; no `if language == "rust"` / `if language == "typescript"` / `if language == "javascript"` server/client branches.
    - Security: Artifact/query paths are confined to the resolver-validated package root; arbitrary external paths, native library loading, network fetches, shell/package-manager execution, raw ops, client JS, CSS, and renderer callbacks are rejected.
  - Approach:
    - Documentation Reviewed:
      - `src/packages/manifest.rs`, `src/packages/service.rs`, `src/packages/record.rs`, `src/packages/permissions.rs`.
      - `docs/reference/primitives/package-security.md` permission/validation checklist.
      - `docs/reference/packages/creating-packages.md` package metadata conventions.
    - Options Considered:
      - Add grammar metadata only inside `clay.contributions`: small diff, but hard to expose as a public primitive/API.
      - Add a runtime-backed `clay:syntax.serverRegisterSyntaxGrammar` API plus manifest validation: more explicit, discoverable, testable, and matches other primitives.
    - Chosen Approach:
      - Add a `SyntaxGrammarContribution` validator/registry and public Clay JS API shape. Manifest contributions and `serverRegisterSyntaxGrammar` share the same Rust validation path.
    - API Notes and Examples:
      ```ts
      import { serverRegisterSyntaxGrammar } from "clay:syntax";

      serverRegisterSyntaxGrammar(manifest, {
        languageId: "rust",
        filePatterns: { extensions: ["rs"] },
        grammar: { kind: "tree-sitter-wasm", path: "./grammars/rust.wasm" },
        queries: { highlights: "./queries/highlights.scm" },
        styleMap: { keyword: "keyword.control", string: "string.quoted", comment: "comment.line" },
        budgets: { timeoutMs: 5000 }
      });
      ```
    - Files to Create/Edit:
      - `src/packages/manifest.rs`: parse/validate `clay.contributions.syntaxGrammars` or equivalent contribution metadata.
      - `src/server/syntax.rs` (new, tentative): grammar registry, contribution structs, validators, diagnostics.
      - `src/server/ops/syntax.rs` (new, tentative): `op_clay_syntax_register_syntax_grammar`.
      - `src/server/ops/mod.rs`, `src/server/js_runtime.rs`, `runtime/js/syntax.ts`: expose curated facade/op.
      - `docs/reference/primitives/registry.md`, `docs/reference/primitives/package-security.md`: document primitive and validation rules.
      - `tests/package_primitive_gate.rs` or new `tests/syntax_grammar.rs`: contribution validation tests.
    - References:
      - `.agents/skills/project-patterns/references/clay-js-api-boundary.md`
      - `.agents/skills/project-patterns/references/package-distribution.md`
  - Test Cases to Write:
    - Valid first-party grammar contribution registers with provenance.
    - External/absolute/parent-traversal artifact or query paths are rejected.
    - Duplicate language/pattern conflicts produce deterministic diagnostics.
    - Package-owned contribution IDs/captures/style maps preserve package prefix/provenance.
  - Completion Evidence:
    - Added `SyntaxGrammarContributionDescriptor` and `PackageContributions::syntax_grammars` in `src/packages/record.rs`; `assemble_package_record` now parses `clay.contributions.syntaxGrammars` with package-owned ID defaulting, `languageId`, extensions/fileNames, `tree-sitter-wasm` grammar path/source, highlights/locals/injections `.scm` query paths, capture-to-known-style-token `styleMap`, optional `timeoutMs`/`maxWindowBytes`, and bounded metadata under `BEHAVIOR_MANIFEST_PAYLOAD_BUDGET_BYTES`.
    - Added load-time security validation in `src/packages/record.rs`: syntax grammars require `parse-document` and `render-decorations`, package-root-confined relative `./` asset paths, `.wasm` grammar artifacts, `.scm` query files, known Clay style tokens only, no raw CSS/colors, no client JavaScript/callbacks/raw ops/native library/download/package-manager/shell authority, no duplicate IDs/language IDs within a package, and no language-specific Rust branches.
    - Added `src/server/syntax.rs` and exported `server::syntax` with server-owned `SyntaxGrammarRegistry` state. `register_package` stages validated package descriptors, commits atomically, indexes by contribution ID/language/extension/filename, preserves package name/version/prefix provenance, and returns typed deterministic duplicate ID/language/extension/filename diagnostics.
    - Added `clay.syntax.serverRegisterSyntaxGrammar` as an accepted API dependency ID in package record validation for the upcoming runtime facade task; this task intentionally stops at manifest metadata and registry state, leaving `src/server/ops/syntax.rs`, `runtime/js/syntax.ts`, and parse/highlight execution to later Phase 18.10 tasks.
    - Updated `docs/reference/primitives/package-security.md`, `docs/reference/packages/creating-packages.md`, `docs/wiki/modules/syntax-grammar-registry.md`, `docs/wiki/index.md`, and `docs/wiki/modules/phase18.10-tree-sitter-grammar-primitive-review.md` with the implemented metadata contract, security boundary, hot-path exclusion, and tests.
    - Added `tests/syntax_grammar.rs` covering valid contribution provenance/budgets, required permissions, external/traversing path rejection, executable/native/client/CSS authority rejection, unknown style token rejection, registry provenance/lookup, and deterministic duplicate language/pattern diagnostics.
    - Updated `src/packages/service.rs` withdrawal counts and `tests/rust_visibility_api_mapping.rs` infrastructure allowlist for the new non-JS registry state.
    - Verification: `cargo fmt --check` clean; `CARGO_TARGET_DIR=target/pi-verify cargo test --test syntax_grammar --test package_loading --test package_primitive_gate --test rust_visibility_api_mapping --test primitives_docs --quiet` passed (169 tests across 5 suites); `git diff --check` clean.

- [x] Implement the server-side Tree-sitter parse/highlight engine on the existing parse/decor path
  - Acceptance Criteria:
    - Functional: Registered syntax grammar contributions can be loaded into a server-side Tree-sitter parser, compile highlight queries, run incremental parsing where possible, extract query captures, convert captures to bounded `DecorationSet` spans, and publish through the existing decoration transport.
    - Performance: Parse/highlight work is `Background`, cancellable, viewport-prioritized, stale-version-rejecting, and bounded by `INCREMENTAL_PARSE_UPDATE_BUDGET_BYTES`, `DECORATION_PAYLOAD_BUDGET_BYTES`, `SYNTAX_CACHE_BUDGET_BYTES`, and parser timeout metadata. No Tree-sitter work runs in Masonry paint/text-event handlers or before client-first local paint.
    - Code Quality: Engine is language-neutral; grammar/query/capture details come from package contributions. Existing `ParseCoordinator`, `ParseWindowSnapshot`, `SyntaxChunkCache`, and `DecorationSet` are reused instead of adding parallel pipelines.
    - Security: Only resolver-validated first-party package grammar artifacts are loaded; malformed grammars/queries fail closed with typed diagnostics and leave editing/fallback modes intact.
  - Approach:
    - Documentation Reviewed:
      - Context7 `/tree-sitter/tree-sitter`: incremental parsing via `Tree::edit`/`Parser::parse(old_tree)`, highlight query examples.
      - Context7 `/websites/rs_tree-sitter`: `Parser::new`, `parser.set_language`, `WasmStore::load_language`, `Query`/`QueryCursor` docs to verify locally after dependency selection.
      - `src/server/parse_coordinator.rs`, `src/protocol/parse.rs`, `src/server/decorations.rs`, `src/protocol/decorations.rs`.
    - Options Considered:
      - New syntax-highlight scheduler: duplicates parse coordinator and cache policy.
      - Reuse `ParseCoordinator` with a Rust `ParseHandler` generated from grammar contribution: smallest generic integration point.
      - Let package JS call `serverPublishDecorations` after parsing: easy for JS parsers, but not Tree-sitter/Rust-owned engine and risks package-runtime bottlenecks.
    - Chosen Approach:
      - Implement a generic Tree-sitter syntax provider that registers a Rust parse handler with `ParseCoordinator` per grammar contribution. Handler stores parser/tree state server-side and emits validated `DecorationSet` values via existing decoration validation/publication paths.
    - API Notes and Examples:
      ```rust
      // docs-derived API shape, verify against local rustdoc after dependency selection
      parser.set_language(&language)?;
      tree.edit(&InputEdit { /* accepted edit byte/point delta */ });
      let next_tree = parser.parse(source_window, old_tree.as_ref())?;
      let query = Query::new(&language, highlights_scm)?;
      let mut cursor = QueryCursor::new();
      ```
    - Files to Create/Edit:
      - `Cargo.toml`, `Cargo.lock`: add Tree-sitter crates/features after local docs verification.
      - `src/server/syntax.rs`: parser/query registry, tree cache, capture extraction, style-map conversion, diagnostics.
      - `src/server/parse_coordinator.rs`: attach generated syntax handlers only if existing API needs a small generic hook.
      - `src/server/decorations.rs`: extend known code style tokens only as generic tokens, not per-language branches.
      - `tests/syntax_grammar.rs` (new, tentative): engine behavior.
      - `tests/editor_performance_invariants.rs`: source guards for no Tree-sitter/package parser calls in paint/text-event paths.
    - References:
      - `.agents/skills/project-patterns/references/protocol-and-performance.md`
      - `docs/wiki/modules/parse-coordinator.md`
      - `docs/wiki/modules/decoration-transport.md`
  - Test Cases to Write:
    - Incremental edit updates reuse prior tree and reject stale document versions.
    - Query capture extraction produces style-tokened syntax `DecorationSet` spans within viewport.
    - Invalid grammar/query/style mapping fails closed and keeps document editable with no highlighting.
    - Slow/high-volume parse is cancelled or times out without blocking edit acknowledgement.
  - Completion Evidence:
    - Added `tree-sitter = "0.24"` and `streaming-iterator = "0.1.9"` runtime dependencies plus `tree-sitter-rust = "0.23"` as a test-only grammar fixture. Verified crate API with `cargo metadata --format-version 1 --no-deps`, `cargo tree -i tree-sitter`, and `CARGO_TARGET_DIR=target/pi-doc cargo doc -p tree-sitter --no-deps --quiet`; local rustdoc/source confirmed `Parser::new`, `Parser::set_language`, `Parser::set_timeout_micros`, `Parser::parse(old_tree)`, `Query::new`, `Query::capture_names`, `QueryCursor::set_timeout_micros`, `QueryCursor::set_byte_range`, and `QueryCursor::captures`.
    - Implemented `TreeSitterSyntaxHandler` in `src/server/syntax.rs` as a language-neutral `ParseHandler`: it accepts a validated `SyntaxGrammarContribution`, resolved `tree_sitter::Language`, and highlight query text; compiles queries once; rejects unmapped captures; enforces contribution `maxWindowBytes`; sets parser/query timeouts from `timeoutMs`; chooses viewport-intersecting `ParseWindowSnapshot`; parses in background through `ParseCoordinator`; reuses cached prior trees for later versions on the same window via `Parser::parse(..., old_tree.as_ref())`; extracts captures with `QueryCursor`; maps captures to package-provenanced `DecorationSpan`s; validates `DecorationSet`s with existing decoration validation; inserts validated sets into existing `SyntaxChunkCache` for near-viewport/LRU `SYNTAX_CACHE_BUDGET_BYTES` enforcement; and returns existing `IncrementalParseUpdate` payloads.
    - Reused the existing parse/decor pipeline without a new scheduler: tests register `TreeSitterSyntaxHandler` with `ParseCoordinator`, schedule via `schedule_parse_with_windows`, and receive validated `IncrementalParseUpdate`/`DecorationSet` output. Stale-version rejection, cancellation, and `INCREMENTAL_PARSE_UPDATE_BUDGET_BYTES` remain enforced by `ParseCoordinator`; decoration payload budget remains enforced by `validate_decoration_set` plus explicit `DECORATION_PAYLOAD_BUDGET_BYTES` serialization checks.
    - Added `tests/syntax_grammar.rs` coverage for query capture extraction into bounded syntax decoration spans, cached-tree reuse for later document versions, invalid query/unmapped capture fail-closed diagnostics, parse-window budget enforcement before parsing, and parse coordinator publication through the existing update channel. Existing parse coordinator tests continue to cover superseded-task cancellation and stale-version rejection.
    - Updated `tests/editor_performance_invariants.rs` to guard paint/layout sources against Tree-sitter parser references, and updated `docs/wiki/modules/syntax-grammar-registry.md` with the parse/highlight engine flow, hot-path exclusion, security boundary, and tests.
    - Verification: `cargo fmt --check` clean; `CARGO_TARGET_DIR=target/pi-verify cargo test --test syntax_grammar --test package_loading --test package_primitive_gate --test parse_coordinator --test decoration_transport --test editor_performance_invariants --test rust_visibility_api_mapping --quiet` passed (125 tests across 7 suites).

- [x] Keep syntax provider selection separate from active major mode and attach it to fallback documents
  - Acceptance Criteria:
    - Functional: A document can have active major mode `core.code` or `core.text` and a separate active syntax grammar such as `rust`, `typescript`, or `javascript`; disabling or invalidating a grammar package falls back to no syntax highlighting without changing editability.
    - Performance: Syntax provider selection runs on open/reload/explicit reclassification/package load or reload, not per keypress; selection stays within `MODE_ACTIVATION_P95_BUDGET_MS` or documented syntax-selection budget.
    - Code Quality: Selection reuses document classification metadata where possible and records why a grammar was selected or skipped for diagnostics/control-center-style discovery.
    - Security: Package-provided grammar metadata cannot override active major mode, command routing, file authority, or behavior manifests.
  - Approach:
    - Documentation Reviewed:
      - `src/packages/modes.rs`: Phase 18.9 classification/fallback modes.
      - `docs/wiki/modules/mode-registry.md`: active mode and fallback rationale.
      - `docs/reference/primitives/registry.md`: `DocumentClassification`, `MajorModeActivation`, `DecorationRange`, `IncrementalParseUpdate` rows.
    - Options Considered:
      - Make grammar packages declare full major modes: violates Phase 18.10 grammar-only scope.
      - Let grammar registry select independently with the same pattern kinds: preserves mode/grammar separation and package-only syntax.
    - Chosen Approach:
      - Add syntax grammar selection state keyed by document/version with deterministic pattern precedence and diagnostics, but do not activate package major modes or publish behavior manifests.
    - API Notes and Examples:
      ```text
      active_major_mode: core.code
      active_syntax_grammar: rust from @clay/rust, selected by extension rs
      edit behavior: core.code manifest
      decoration provider: rust grammar contribution
      ```
    - Files to Create/Edit:
      - `src/server/syntax.rs`: selection state and diagnostics.
      - `src/server/connection.rs`: schedule grammar parse/highlight after open/reload using selected grammar.
      - `src/server/command_execution.rs` or mode discovery output (tentative): include active syntax grammar explanation only if generic discovery surface already supports extension.
      - `tests/syntax_grammar.rs`: mode/grammar separation tests.
      - `docs/wiki/modules/mode-registry.md`: document active syntax grammar as separate from active major mode.
    - References:
      - `.agents/skills/project-patterns/references/language-capability-sequencing.md`
  - Test Cases to Write:
    - `.rs` opens as `core.code` plus active syntax grammar `rust` when `@clay/rust` is loaded.
    - Same `.rs` opens/editable as `core.code` with no active syntax grammar when package is disabled/invalid/unloaded.
    - A package grammar contribution cannot change major mode, behavior version, or command routing.
  - Completion Evidence:
    - Implemented active syntax grammar selection in `src/server/syntax.rs`: `SyntaxGrammarRegistry::select_for_document` accepts existing `DocumentClassificationInput`, already-selected `MajorModeActivation`, and document version, then records `SyntaxGrammarSelection` keyed by document ID without recomputing or changing major-mode activation.
    - Added `ActiveSyntaxGrammar`, `SyntaxGrammarSelection`, and `SyntaxGrammarPatternKind` diagnostics recording selected grammar provenance, matched pattern kind, active major mode ID, and behavior version. Selection uses only supplied open-document path metadata with exact filename before extension; it performs no filesystem scan, package JavaScript execution, command routing mutation, behavior-manifest publication, or mode activation.
    - Added `tests/syntax_grammar.rs` coverage verifying `.rs` keeps active major mode `core.code` while selecting `rust` syntax grammar when loaded, exact filename grammar selection can attach to `core.text`, no loaded grammar falls back to no syntax grammar while preserving editability rationale, and grammar selection cannot override mode ID or behavior version.
    - Updated `docs/wiki/modules/syntax-grammar-registry.md` and `docs/wiki/modules/mode-registry.md` to document active syntax grammar as separate from active major mode, with no hot-path selection and no authority expansion.
    - Verification: `cargo fmt --check` clean; `git diff --check` clean; `CARGO_TARGET_DIR=target/pi-verify cargo test --test syntax_grammar --test package_primitive_gate --test command_execution --test editor_performance_invariants --test rust_visibility_api_mapping --test primitives_docs --quiet` passed (167 tests across 6 suites).

- [x] Create grammar-only first-party packages `@clay/rust`, `@clay/typescript`, and `@clay/javascript`
  - Acceptance Criteria:
    - Functional: Each package declares a package prefix, syntax grammar contribution, file patterns, grammar artifact/source metadata, highlight query files, optional locals/injections metadata where implemented, style-token mapping, docs, API dependencies, and performance metadata.
    - Performance: Package assets and metadata stay bounded; grammar/query validation runs at package load; parse/highlight output stays viewport-bounded.
    - Code Quality: Packages are grammar-only: no full major modes, language-specific commands, completion providers, SDUI panels, package UI, bespoke behavior manifests, or Rust branches.
    - Security: Packages request only required inert/parse/render capabilities; they do not request filesystem, network, shell, AI, WASM, raw-op, native-ui, client-runtime, package-manager, or package-control authority.
  - Approach:
    - Documentation Reviewed:
      - `packages/markdown/package.json`: current first-party package shape and docs conventions.
      - `docs/reference/packages/creating-packages.md`: package authoring rules.
      - `docs/reference/primitives/package-security.md`: capability vocabulary and prohibited authorities.
    - Options Considered:
      - One `@clay/code-grammars` package containing all grammars: fewer packages, but hides provenance and upgrade path.
      - Separate packages per language: matches roadmap, future language package expansion, and provenance diagnostics.
    - Chosen Approach:
      - Add three separate first-party packages with only grammar/query assets and one load entry that calls the generic syntax API.
    - API Notes and Examples:
      ```js
      import { loadPackage } from "clay:packages";
      await loadPackage("@clay/rust");
      await loadPackage("@clay/typescript");
      await loadPackage("@clay/javascript");
      ```
    - Files to Create/Edit:
      - `packages/rust/package.json`, `packages/rust/dist/load.js`, `packages/rust/docs/index.md`, `packages/rust/queries/highlights.scm`, `packages/rust/grammars/*`.
      - `packages/typescript/package.json`, `dist/load.js`, `docs/index.md`, `queries/highlights.scm`, `grammars/*`.
      - `packages/javascript/package.json`, `dist/load.js`, `docs/index.md`, `queries/highlights.scm`, `grammars/*`.
      - `tests/fixtures/configuration/syntax-grammars-init.js`: explicit package loading fixture.
      - `tests/package_loading.rs` or `tests/syntax_grammar.rs`: first-party package validation.
    - References:
      - `.agents/skills/project-patterns/references/package-distribution.md`
      - `.agents/skills/project-patterns/references/language-capability-sequencing.md`
  - Test Cases to Write:
    - Each package validates and loads with no mode/command/completion/UI contributions.
    - Package docs and API dependencies are present and linked.
    - Source scan rejects language-specific Rust branches while allowing package asset paths/names.
  - Completion Evidence:
    - Added grammar-only first-party package scaffolds under `packages/rust/`, `packages/typescript/`, and `packages/javascript/` with `package.json`, `dist/load.js`, `dist/index.js`, `docs/index.md`, `queries/highlights.scm`, and `grammars/README.md` artifact notes.
    - Each package declares one `clay.contributions.syntaxGrammars` entry with package-owned `apiPrefix`, `languageId`, file patterns, `tree-sitter-wasm` grammar path/source metadata, highlight query path, capture-to-known-style-token map, docs path, API dependency `clay.syntax.serverRegisterSyntaxGrammar`, performance metadata, and budgets.
    - Each package is grammar-only: `clay.modes` is empty and tests verify no commands, configuration, key routing, text transforms, SDUI, legacy decorations, UI panels/components/overlays, theme tokens, input contributions, UI state scopes, layout overrides, package options, completions, or behavior manifests are declared.
    - Security scope is limited to `parse-document` and `render-decorations`; no filesystem, network, shell, AI, WASM-authority, raw-op, native-ui, client-runtime, package-manager, package-control, or workspace mutation authority is requested.
    - Added explicit end-user loading fixture `tests/fixtures/configuration/syntax-grammars-init.js` using only `loadPackage("@clay/rust")`, `loadPackage("@clay/typescript")`, and `loadPackage("@clay/javascript")`, with no manual `serverLoadPackage` or `serverRegisterSyntaxGrammar` calls.
    - Added public reference docs `docs/reference/packages/rust.md`, `docs/reference/packages/typescript.md`, and `docs/reference/packages/javascript.md`; linked them from `docs/index.md` and `docs/reference/packages/creating-packages.md`; updated `docs/wiki/modules/syntax-grammar-registry.md` with first-party grammar package scope.
    - Added `tests/syntax_grammar.rs` coverage validating the on-disk package manifests via `assemble_package_record`, package-only grammar contribution shape, docs/API dependency links, load-entry/default export presence, highlight query assets, explicit fixture loading, and no language-specific Rust branches in syntax registry/record code.
    - Verification: `cargo fmt --check` clean; `git diff --check` clean; `node --check packages/rust/dist/load.js`, `node --check packages/typescript/dist/load.js`, and `node --check packages/javascript/dist/load.js` clean; `CARGO_TARGET_DIR=target/pi-verify cargo test --test syntax_grammar --test package_loading --test package_loading_docs --test package_primitive_gate --test primitives_docs --quiet` passed (196 tests across 5 suites).

- [x] Validate highlight queries, captures, style tokens, diagnostics, and fallback behavior
  - Acceptance Criteria:
    - Functional: Highlight query files compile, query capture names map to known Clay style tokens, unknown/unsupported captures produce actionable diagnostics, and valid captures become `DecorationKind::Syntax` spans.
    - Performance: Capture extraction and decoration shaping are viewport-bounded; oversize query/capture output is rejected before IPC/client publication.
    - Code Quality: Style-token mapping is generic and documented; tokens are reusable code syntax tokens, not package-specific CSS or raw color strings.
    - Security: Queries cannot inject executable code, CSS, native rendering callbacks, raw ops, or client-side JavaScript; invalid query files degrade to no highlighting for that package.
  - Approach:
    - Documentation Reviewed:
      - Tree-sitter highlighting query docs from Context7: `highlights.scm` capture examples.
      - `src/server/decorations.rs::is_known_style_token` and `docs/reference/primitives/rendering-strategy.md` style-token rules.
      - `docs/wiki/modules/decoration-transport.md` known inert style-token allowlist.
    - Options Considered:
      - Accept any query capture as a style token: flexible, but allows arbitrary style strings.
      - Require package style-map entries to translate captures into Clay-known tokens: safe and deterministic.
    - Chosen Approach:
      - Compile queries at load time, extract/validate capture names against the package style map, and emit only known Clay style tokens in `DecorationSet`.
    - API Notes and Examples:
      ```scheme
      (line_comment) @comment
      (string_literal) @string
      ["fn" "return"] @keyword
      ```
      ```json
      { "comment": "comment.line", "string": "string.quoted", "keyword": "keyword.control" }
      ```
    - Files to Create/Edit:
      - `src/server/syntax.rs`: query compile/capture mapping/diagnostic code.
      - `src/server/decorations.rs`: extend known generic code tokens only if required by real query output.
      - `docs/reference/primitives/rendering-strategy.md`: document syntax grammar capture/style-map rules.
      - `tests/syntax_grammar.rs`: query diagnostics and payload tests.
    - References:
      - `.agents/skills/project-patterns/references/protocol-and-performance.md`
  - Test Cases to Write:
    - Valid query maps captures to `keyword.control`, `string.quoted`, `comment.line`, `punctuation.definition`, and other approved code tokens.
    - Unknown capture without style-map entry fails load or disables that query with diagnostic.
    - Raw CSS/color/style strings and unknown tokens are rejected.
    - Decoration payload over `DECORATION_PAYLOAD_BUDGET_BYTES` is rejected.
  - Completion Evidence:
    - `TreeSitterSyntaxHandler::new` compiles highlight queries once and fails closed with typed/actionable diagnostics for invalid query syntax and unmapped captures such as `@function`.
    - Valid Rust query coverage now maps `@keyword`, `@string`, `@comment`, and `@punctuation` captures to known Clay tokens `keyword.control`, `string.quoted`, `comment.line`, and `punctuation.definition` and emits only `DecorationKind::Syntax` spans.
    - First-party `@clay/rust`, `@clay/typescript`, and `@clay/javascript` package manifests and `queries/highlights.scm` files include the approved `punctuation` capture mapping alongside keyword/string/comment mappings.
    - Package metadata validation still rejects unknown style tokens, raw CSS/color strings, external/traversing query/grammar paths, native/executable authority fields, raw ops, and client JavaScript before handler creation.
    - Viewport extraction uses `QueryCursor::set_byte_range`; captures are clamped to the requested viewport; per-viewport capture output above the syntax span cap now returns `TreeSitterSyntaxError::CaptureLimitExceeded` instead of silently truncating or publishing partial oversize output.
    - `DecorationSet` validation and `DECORATION_PAYLOAD_BUDGET_BYTES` serialization checks now run before `SyntaxChunkCache` insertion or parse/decor publication.
    - Docs updated in `docs/reference/primitives/rendering-strategy.md` and `docs/wiki/modules/syntax-grammar-registry.md` to describe capture/style-map validation, diagnostics, payload/capture rejection, and fail-closed fallback.
    - Verification: `cargo fmt --check` clean; `git diff --check` clean; `CARGO_TARGET_DIR=target/pi-verify cargo test --test syntax_grammar --test package_loading --test package_loading_docs --test package_primitive_gate --test primitives_docs --test rust_visibility_api_mapping --test editor_performance_invariants --quiet` passed (217 tests across 7 suites).

- [x] Define and verify the package default `init.js` loading experience
  - Acceptance Criteria:
    - Functional: Grammar packages are explicitly loaded from `~/.config/clay/init.js` with one-line `loadPackage("@clay/<language>")` calls; no grammar package becomes a silent behavior-changing default.
    - Performance: Loading one or more grammar packages does package validation once at startup/config load and does not add work to ordinary typing hot paths.
    - Code Quality: Normal syntax highlighting works after the one-line load without copied package manifests, manual primitive registration, test-only fixtures, or direct raw op calls in user config.
    - Security: `loadPackage` does not grant package-enable/disable, filesystem, network, shell, AI, native UI, raw-op, client-runtime, or package-manager authority beyond existing constrained package loading.
  - Approach:
    - Documentation Reviewed:
      - `.agents/skills/project-patterns/references/package-distribution.md` package default loading rule.
      - `docs/reference/clay-js-api/packages/load-package.md`.
      - `docs/wiki/modules/package-loading.md`.
    - Options Considered:
      - Auto-load shipped grammar packages for known extensions: convenient, but violates explicit package loading and changes behavior silently.
      - Require explicit one-line `loadPackage` per grammar package: matches package policy and keeps provenance clear.
    - Chosen Approach:
      - Document and test explicit one-line loading for first-party grammar packages; no automatic language package load in core.
    - API Notes and Examples:
      ```js
      import { loadPackage } from "clay:packages";
      await loadPackage("@clay/rust");
      await loadPackage("@clay/typescript");
      await loadPackage("@clay/javascript");
      ```
    - Files to Create/Edit:
      - `docs/reference/packages/creating-packages.md`: grammar-only package loading example.
      - `docs/reference/packages/rust.md`, `docs/reference/packages/typescript.md`, `docs/reference/packages/javascript.md` (or package-local docs linked from package guide): end-user setup.
      - `tests/package_loading_docs.rs`: one-line loading docs/fixture check.
      - `tests/fixtures/configuration/syntax-grammars-init.js`: default fixture.
    - References:
      - `decision-logs/2026-06-09-0219-explicit-init-js-package-loading-with-one-line-defaults.md`
  - Test Cases to Write:
    - Fixture `init.js` loading all three packages enables syntax highlighting for matching files.
    - Without explicit package load, files remain editable under `core.code`/`core.text` with no syntax grammar.
    - Docs do not present `serverLoadPackage(packageJson)` or manual `serverRegisterSyntaxGrammar` calls as ordinary end-user setup.
  - Completion Evidence:
    - `tests/fixtures/configuration/syntax-grammars-init.js` defines the default user-facing setup with only `loadPackage("@clay/rust")`, `loadPackage("@clay/typescript")`, and `loadPackage("@clay/javascript")`; it contains no copied package manifests, raw ops, `serverLoadPackage`, or manual `serverRegisterSyntaxGrammar` calls.
    - Added runtime coverage `src/server/js_runtime.rs::syntax_grammar_packages_default_load_from_init_js` that evaluates an actual `init.js`, loads all three packages through `clay:packages.loadPackage`, and verifies each summary has no modes, only `parse-document` + `render-decorations`, and one `syntaxGrammars` contribution.
    - `op_clay_packages_load_package_by_specifier` now includes `contributions.syntaxGrammars` in the returned summary so init/default-load tests can verify grammar metadata was enabled through the normal package resolver.
    - Public docs updated in `docs/reference/clay-js-api/packages/load-package.md` and `docs/reference/packages/creating-packages.md` to show the explicit grammar package `init.js` lines; `docs/wiki/modules/syntax-grammar-registry.md` documents the same fixture, no-auto-load policy, and no extra authority from `init.js`.
    - Added `tests/package_loading_docs.rs::syntax_grammar_packages_document_explicit_init_js_loading` to pin one-line docs/fixture coverage, no silent auto-load language, no extra authority language, no low-level facade/raw-op end-user setup, runtime test presence, and the `syntaxGrammars` summary count.
    - Existing `tests/syntax_grammar.rs` covers the no-explicit-load fallback: documents remain editable under their active `core.code`/`core.text` major mode with `active_syntax_grammar = None` when no grammar package is loaded.
    - Verification: `cargo fmt --check` clean; `git diff --check` clean; `CARGO_TARGET_DIR=target/pi-verify cargo test syntax_grammar_packages_default_load_from_init_js --quiet` passed; `CARGO_TARGET_DIR=target/pi-verify cargo test --test package_loading_docs --test syntax_grammar --test package_loading --quiet` passed (84 tests across 3 suites).

- [x] Add syntax highlighting performance, security, and fallback verification
  - Acceptance Criteria:
    - Functional: Rust, TypeScript, and JavaScript fixture files produce syntax `DecorationSet` values when packages are loaded; disabled/invalid packages fall back to editable `core.code`/`core.text` with no highlighting.
    - Performance: Parse/highlight tasks are cancellable and stale results are dropped; decoration payloads, syntax cache, parse update sizes, and no-hot-path invariants are tested against typed budgets.
    - Code Quality: Focused tests cover package validation, query validation, grammar selection, parse/highlight output, protocol round trips, docs registry, and source guards against language-specific Rust branches.
    - Security: Tests reject arbitrary grammar paths, third-party/native artifact loading, malformed query/style data, raw ops, client JS, and any package attempts to declare full mode/command/completion/UI contributions in grammar-only packages.
  - Approach:
    - Documentation Reviewed:
      - `docs/wiki/modules/maintenance-validation.md` and `.agents/skills/project-patterns/references/maintenance-validation.md`.
      - `tests/decoration_transport.rs`, `tests/parse_coordinator.rs`, `tests/editor_performance_invariants.rs`, `tests/package_loading_docs.rs`.
    - Options Considered:
      - Only test unit-level grammar parsing: fast, but misses package and protocol boundaries.
      - Run focused integration plus docs/API/security gates: needed for a public primitive.
    - Chosen Approach:
      - Add one focused syntax test suite plus extend existing docs/API/performance gates.
    - API Notes and Examples:
      ```bash
      cargo fmt --check
      CARGO_TARGET_DIR=target/pi-verify cargo test --test syntax_grammar --quiet
      CARGO_TARGET_DIR=target/pi-verify cargo test --test parse_coordinator --quiet
      CARGO_TARGET_DIR=target/pi-verify cargo test --test decoration_transport --quiet
      CARGO_TARGET_DIR=target/pi-verify cargo test --test editor_performance_invariants --quiet
      CARGO_TARGET_DIR=target/pi-verify cargo test --test package_loading_docs --quiet
      CARGO_TARGET_DIR=target/pi-verify cargo test --test clay_js_api_inventory --quiet
      CARGO_TARGET_DIR=target/pi-verify cargo test --test clay_js_doc_registry --quiet
      CARGO_TARGET_DIR=target/pi-verify cargo test --test primitives_docs --quiet
      ```
    - Files to Create/Edit:
      - `tests/syntax_grammar.rs`: end-to-end syntax grammar contribution tests.
      - `tests/fixtures/syntax/rust.rs`, `typescript.ts`, `javascript.js`: syntax fixtures.
      - `tests/editor_performance_invariants.rs`: hot-path source guard additions.
      - `tests/performance_protocol.rs`: decoration/query payload checks if needed.
      - `docs/development/launch-and-gui-smoke.md`: manual syntax smoke steps.
    - References:
      - `.agents/skills/project-patterns/references/maintenance-validation.md`
  - Test Cases to Write:
    - Syntax highlight smoke for `.rs`, `.ts`, `.js` fixtures after explicit package load.
    - Disabled/invalid package fallback to `core.code`/`core.text` with no panic and no file-open failure.
    - Stale parse result rejection after edit/version advance.
    - Source guard: no Tree-sitter/parser/package calls in Masonry paint/text-event handlers and no language-specific Rust branches.
    - Manual smoke: launch GUI with syntax package fixture, open `.rs`, `.ts`, `.js`, type/edit/scroll, verify responsive editing and visible highlights.
  - Completion Evidence:
    - Added dev-only grammar fixtures and runtime grammar coverage: `tests/fixtures/syntax/rust.rs`, `tests/fixtures/syntax/typescript.ts`, and `tests/fixtures/syntax/javascript.js`; `tests/syntax_grammar.rs::first_party_syntax_fixtures_produce_bounded_decoration_sets` loads each first-party package record, compiles the package highlight query against the corresponding Tree-sitter grammar crate, parses the fixture through the generic `TreeSitterSyntaxHandler`, and asserts bounded `DecorationSet` output with keyword/string/comment spans and package provenance.
    - Added dev dependencies `tree-sitter-javascript = 0.23.1` and `tree-sitter-typescript = 0.23.2` for fixture-only verification; `cargo tree -i tree-sitter-javascript` and `cargo tree -i tree-sitter-typescript` show both are dev-dependencies of `clay` only.
    - Tightened first-party highlight queries so the shipped Rust/TypeScript/JavaScript query files compile against their actual grammar crates; the fixture test would fail on invalid captures or unmapped style tokens.
    - Added `syntax_grammar_rejects_third_party_packages_in_phase18_10` and a shared validator guard in `src/packages/record.rs` rejecting non-`@clay/*` `SyntaxGrammarContribution` manifests, preserving Phase 18.10 first-party-only grammar scope.
    - Existing and rerun coverage verifies fallback and stale/performance behavior: `syntax_provider_selection_falls_back_to_no_highlighting_without_changing_mode` keeps `core.code` editable with no active grammar; `tree_sitter_handler_publishes_through_parse_coordinator_and_rejects_stale_results`, `tests/parse_coordinator.rs`, and `tests/decoration_transport.rs` cover background scheduling, cancellation/stale drops, payload/window/cache budgets, and validated decoration transport; `tests/editor_performance_invariants.rs` guards paint/layout hot paths against Tree-sitter/package parser calls; `first_party_grammar_packages_do_not_add_language_specific_rust_branches` guards generic server code.
    - Documentation/manual smoke updated in `docs/wiki/modules/syntax-grammar-registry.md`, `docs/reference/primitives/package-security.md`, and `docs/development/launch-and-gui-smoke.md` with first-party-only rejection, real fixture coverage, focused verification commands, and GUI syntax smoke steps.
    - Verification: `cargo fmt --check` clean; `git diff --check` clean; `CARGO_TARGET_DIR=target/pi-verify cargo test --test syntax_grammar --test parse_coordinator --test decoration_transport --test editor_performance_invariants --test package_loading_docs --test clay_js_api_inventory --test clay_js_doc_registry --test primitives_docs --quiet` passed (253 tests across 8 suites); `CARGO_TARGET_DIR=target/pi-verify cargo test --test package_loading --quiet` passed (37 tests); `node --check` passed for all three grammar package load entries.

- [x] Update package authoring, primitive, and reference documentation for grammar packages
  - Acceptance Criteria:
    - Functional: Public docs explain how a package provides a grammar-only syntax contribution, how active syntax grammar differs from active major mode, how first-party packages are loaded, and what is unsupported until later phases.
    - Performance: Docs include parse/highlight background policy, viewport/payload/cache budgets, and no-hot-path constraints.
    - Code Quality: Docs link authoritative Clay JS API pages instead of duplicating full API reference; implementation wiki pages explain internals.
    - Security: Docs state first-party-only artifact loading, package-root path confinement, no arbitrary third-party/native artifact loading, and no hidden filesystem/network/shell/AI/WASM/raw-op/client-JS authority.
  - Approach:
    - Documentation Reviewed:
      - `docs/reference/packages/creating-packages.md`.
      - `docs/reference/primitives/index.md`, `registry.md`, `package-security.md`, `parse-update-strategy.md`, `rendering-strategy.md`.
      - `docs/index.md` master docs index.
    - Options Considered:
      - Put docs only in package README files: easy, but not discoverable by Clay docs registry.
      - Update package guide, primitive refs, per-package docs, docs index, and registry: more complete and required by documentation-as-code.
    - Chosen Approach:
      - Keep authoritative public usage in `docs/reference/**` and package-local docs linked from there; keep internals in `docs/wiki/**` final task.
    - API Notes and Examples:
      ```markdown
      docs/reference/primitives/registry.md#syntaxgrammarcontribution
      docs/reference/packages/creating-packages.md#grammar-only-packages
      docs/reference/clay-js-api/syntax/server-register-syntax-grammar.md
      ```
    - Files to Create/Edit:
      - `docs/reference/primitives/index.md`, `registry.md`, `package-security.md`, `parse-update-strategy.md`, `rendering-strategy.md`.
      - `docs/reference/packages/creating-packages.md`.
      - `docs/reference/packages/rust.md`, `typescript.md`, `javascript.md` (or equivalent package pages).
      - `docs/index.md`: link new docs.
      - Generated docs registry artifacts from `cargo run --bin update-doc-registry` if Clay JS API docs change.
      - `tests/package_loading_docs.rs`, `tests/clay_js_doc_registry.rs`, `tests/primitives_docs.rs`.
    - References:
      - `.agents/skills/project-patterns/references/documentation-as-code.md`
      - `.agents/skills/project-patterns/references/doc-registry-tests.md`
  - Test Cases to Write:
    - Docs registry check: new syntax API/package docs are linked from `docs/index.md` and generated registry is fresh.
    - Package guide check: grammar-only packages do not document full modes/commands/completions as implemented in this phase.
  - Completion Evidence:
    - Public package docs exist and are linked from `docs/index.md`: `docs/reference/packages/rust.md`, `docs/reference/packages/typescript.md`, and `docs/reference/packages/javascript.md`; package-local docs under `packages/{rust,typescript,javascript}/docs/index.md` mirror the grammar-only contract.
    - `docs/reference/packages/creating-packages.md` documents the Phase 18.10 grammar-only authoring contract with `clay.contributions.syntaxGrammars`, `tree-sitter-wasm` package-root-confined assets, `.scm` query paths, style maps including `punctuation.definition`, explicit `loadPackage("@clay/*")` setup, first-party-only scope, unsupported full mode/command/completion/UI behavior, and Background/no-hot-path parse/decor/cache budgets.
    - Primitive/reference docs updated: `docs/reference/primitives/index.md` links the correct Phase 18.10 authoring anchor; `registry.md`, `package-security.md`, `parse-update-strategy.md`, and `rendering-strategy.md` document active syntax grammar vs active major mode, first-party-only/package-root/native/third-party restrictions, background viewport-prioritized parse/highlight, and `INCREMENTAL_PARSE_UPDATE_BUDGET_BYTES`/`DECORATION_PAYLOAD_BUDGET_BYTES`/`SYNTAX_CACHE_BUDGET_BYTES` constraints.
    - Implementation wiki `docs/wiki/modules/syntax-grammar-registry.md` documents the internal registry/selection/handler flow and links related parse/decor/package-loading pages; manual GUI syntax smoke steps are in `docs/development/launch-and-gui-smoke.md`.
    - Added `tests/package_loading_docs.rs::grammar_package_reference_docs_cover_authoring_primitive_security_and_performance` so docs/index links, package reference docs, package-local docs, primitive docs, performance/security/no-hot-path language, first-party-only scope, and avoidance of low-level end-user setup remain pinned.
    - Verification: `cargo fmt --check` clean; `git diff --check` clean; `CARGO_TARGET_DIR=target/pi-verify cargo test --test package_loading_docs --test primitives_docs --test clay_js_doc_registry --quiet` passed (142 tests across 3 suites).

- [x] Create or verify Clay JS APIs for public programmatic surfaces
  - Acceptance Criteria:
    - Functional: Every public programmatic surface introduced or changed by this phase has an explicit `deno_core` op wrapper, stable Clay JS/TS facade export, Markdown API docs, generated registry entry, and lookup coverage; internal Rust helpers are private or `pub(crate)`.
    - Performance: API docs and validation state call out background/no-hot-path behavior and payload/cache/time budgets.
    - Code Quality: API naming follows Clay module/export/stable-ID/user-facing-name conventions; raw `Deno.core.ops.op_*` calls are not user-facing.
    - Security: API docs and validators reject raw ops, arbitrary artifact paths, executable callbacks, client JS, native handles, filesystem/network/shell/AI/WASM authority, and third-party grammar artifact loading in this phase.
  - Approach:
    - Documentation Reviewed:
      - `.agents/skills/create-plan/references/clay.md` Clay JS API task.
      - `.agents/skills/project-patterns/references/clay-js-api-boundary.md`, `clay-js-api-naming.md`, `clay-js-api-schema.md`, `documentation-as-code.md`, `doc-registry-tests.md`.
      - `docs/reference/clay-js-api/api-inventory.toml`.
    - Options Considered:
      - Keep syntax registration internal only: smaller now, but packages cannot provide grammars through documented APIs.
      - Promote `clay:syntax.serverRegisterSyntaxGrammar`: explicit primitive/API and registry coverage.
    - Chosen Approach:
      - Add or verify `clay:syntax` API docs/inventory/facade/op for grammar registration and keep selection/execution internals non-public unless user-facing discovery requires a separate API.
    - API Notes and Examples:
      ```text
      JS module: clay:syntax
      JS export: serverRegisterSyntaxGrammar
      Stable ID: clay.syntax.serverRegisterSyntaxGrammar
      User-facing name: Register Syntax Grammar
      Permission: parse-document + render-decorations (or documented grammar-specific capability if added)
      ```
    - Files to Create/Edit:
      - `runtime/js/syntax.ts`: facade export.
      - `src/server/ops/syntax.rs`: op wrapper.
      - `src/server/js_runtime.rs`: curated facade/module loader constant.
      - `docs/reference/clay-js-api/syntax/server-register-syntax-grammar.md`: API docs.
      - `docs/reference/clay-js-api/api-inventory.toml`, `docs/index.md`, generated registry artifacts.
      - `tests/clay_js_api_inventory.rs`, `tests/clay_js_doc_registry.rs`, `tests/rust_visibility_api_mapping.rs`.
    - References:
      - `decision-logs/2026-05-08-1509-clay-js-api-facade-for-rust-functions.md`
      - `decision-logs/2026-05-08-1840-clay-js-api-discovery-keybindings-custom-properties.md`
  - Test Cases to Write:
    - API inventory/docs/registry tests fail if `serverRegisterSyntaxGrammar` docs, index link, facade, op, key binding metadata, custom properties, or lookup entry are missing/stale.
    - Rust visibility mapping fails if new public Rust functions bypass Clay JS API or should be made private/pub(crate).
  - Completion Evidence:
    - Added runtime-backed public facade `runtime/js/syntax.ts::serverRegisterSyntaxGrammar` and embedded runtime facade `src/server/js_runtime.rs::CLAY_FACADE_SYNTAX`, exposed as `clay:syntax` through the curated module loader.
    - Added `src/server/ops/syntax.rs::op_clay_syntax_register_syntax_grammar` and wired it through `src/server/ops/mod.rs`; the op rejects executable/raw authority fields, reuses `assemble_package_record` validation, requires `parse-document` + `render-decorations`, preserves Phase 18.10 first-party-only `@clay/*` grammar scope, and registers validated metadata in `SyntaxGrammarRegistry`.
    - Updated first-party grammar package load entries `packages/rust/dist/load.js`, `packages/typescript/dist/load.js`, and `packages/javascript/dist/load.js` to import `clay:syntax` and register the same inert grammar contract declared in package metadata; ordinary user setup remains one-line `loadPackage("@clay/<language>")`.
    - Added authoritative API docs at `docs/reference/clay-js-api/syntax/server-register-syntax-grammar.md`, linked from `docs/index.md`, added `clay.syntax.serverRegisterSyntaxGrammar` to `docs/reference/clay-js-api/api-inventory.toml`, regenerated `docs/generated/clay-js-api-registry.json`, and updated wiki/docs references to remove stale “later facade/op” language.
    - Added deterministic coverage: `src/server/js_runtime.rs::syntax_facade_registers_grammar_metadata_without_raw_ops`, `syntax_facade_rejects_raw_authority_and_third_party_grammars`, and `load_package_registers_first_party_syntax_grammars`; `tests/clay_js_api_inventory.rs::syntax_grammar_registration_api_has_public_facade_op_inventory_and_docs`; `tests/clay_js_doc_registry.rs::syntax_grammar_public_api_has_docs_registry_and_security_metadata`; and `tests/clay_js_facade_layout.rs` now includes `runtime/js/syntax.ts`.
    - Rust visibility/API mapping remains covered by `tests/rust_visibility_api_mapping.rs`; new op functions are not raw user-facing APIs and are mapped in inventory/docs.
    - Verification: `cargo fmt --check` clean; `git diff --check` clean; `node --check` passed for all three grammar package load entries; `cargo run --bin update-doc-registry` updated the generated registry; `CARGO_TARGET_DIR=target/pi-verify cargo test --lib --quiet` passed (480 tests); `CARGO_TARGET_DIR=target/pi-verify cargo test --test clay_js_api_inventory --test clay_js_facade_layout --test clay_js_doc_registry --test rust_visibility_api_mapping --test syntax_grammar --test package_loading --test package_loading_docs --test primitives_docs --quiet` passed (269 tests across 8 suites).

- [x] Create or verify Clay configuration APIs
  - Acceptance Criteria:
    - Functional: User-visible syntax behavior introduced by this phase is either configured through existing documented Clay JS APIs (`loadPackage`, package options/layout overrides if applicable) or has a documented new configuration API with registry coverage.
    - Performance: Configuration evaluation occurs during `init.js`/package load/config reload only and never in typing/paint/layout hot paths.
    - Code Quality: No hidden JSON/TOML/ad hoc settings are added for grammar packages, grammar selection, parse policy, style-token mapping, or syntax theme overrides.
    - Security: Configuration does not implicitly grant filesystem, network, shell, package-manager, AI, WASM, raw-op, native-widget, client-runtime, package-enable/disable, or third-party grammar authority.
  - Approach:
    - Documentation Reviewed:
      - `.agents/skills/create-plan/references/clay.md` configuration task.
      - `.agents/skills/project-patterns/references/configuration-system.md`.
      - `docs/reference/clay-js-api/configuration/set-package-option.md`.
      - `docs/reference/packages/creating-packages.md` package option rules.
    - Options Considered:
      - Add user settings for preferred syntax grammar/style maps now: speculative, duplicates package load and theme-token surfaces.
      - Use explicit `loadPackage` only for this phase and defer extra config until user-facing override need exists: smallest working path.
    - Chosen Approach:
      - Verify explicit package loading is the only required user configuration. Add no new syntax configuration API unless implementation introduces behavior-changing user options.
    - API Notes and Examples:
      ```js
      import { loadPackage } from "clay:packages";
      await loadPackage("@clay/rust");
      ```
    - Files to Create/Edit:
      - `docs/reference/clay-js-api/packages/load-package.md`: ensure examples mention grammar-only packages if useful.
      - `docs/reference/packages/creating-packages.md`: no hidden syntax config keys.
      - `tests/package_loading_docs.rs`, `tests/clay_js_api_inventory.rs`: config/docs coverage.
    - References:
      - `decision-logs/2026-05-08-1841-configuration-through-init-js-and-clay-js-apis.md`
  - Test Cases to Write:
    - Docs/tests reject hidden keys such as `syntax.preferredGrammar`, `treeSitter.grammarPath`, raw style-map overrides, or auto-load flags unless a documented API is added.
    - `init.js` fixture with only `loadPackage` calls enables first-party syntax packages.
  - Completion Evidence:
    - Verified Phase 18.10 does not need a new user-facing syntax configuration API: explicit `loadPackage("@clay/rust")`, `loadPackage("@clay/typescript")`, and `loadPackage("@clay/javascript")` remain the only end-user configuration surface for first-party grammar packages.
    - Added `docs/reference/clay-js-api/configuration.md` Phase 18.10 syntax grammar configuration review documenting no new `clay:configuration` API, no hidden JSON/TOML/ad hoc syntax keys, no automatic grammar package load, package-owned manifest metadata for grammar paths/query/style maps, load/open/reload/reclassification-only evaluation, no hot-path JavaScript, and no authority expansion.
    - Updated `docs/reference/packages/creating-packages.md` and `docs/wiki/modules/syntax-grammar-registry.md` to reject hidden syntax configuration keys for preferred grammar selection, grammar paths, style maps, capture styles, and auto-load behavior unless a future documented Clay JS API with custom properties and registry coverage is added.
    - Added `tests/package_loading_docs.rs::syntax_grammar_configuration_review_uses_only_documented_clay_js_apis` covering the configuration review text, one-line `loadPackage` docs, end-user fixture shape, package authoring/wiki hidden-key rejection, and absence of hidden syntax configuration IDs/keys from `api-inventory.toml` and the generated registry.
    - Reused existing runtime coverage for the fixture path: `src/server/js_runtime.rs::syntax_grammar_packages_default_load_from_init_js` executes an `init.js` with only the three `loadPackage` calls and verifies syntax grammar package summaries.
    - Verification: `cargo fmt --check` clean; `git diff --check` clean; `CARGO_TARGET_DIR=target/pi-verify cargo test --test package_loading_docs --test clay_js_api_inventory --test clay_js_doc_registry --test package_loading --quiet` passed (145 tests across 4 suites); `rg -n "syntax\\.preferredGrammar|treeSitter\\.grammarPath|autoLoadSyntaxPackages|setSyntaxGrammar|setSyntaxStyleMap|setTreeSitterGrammarPath" docs/reference/clay-js-api/api-inventory.toml docs/generated/clay-js-api-registry.json runtime src tests/fixtures/configuration/syntax-grammars-init.js -g '!target' || true` returned 0 matches.

- [x] Run final verification and manual syntax smoke
  - Acceptance Criteria:
    - Functional: Grammar contributions, package loading, syntax selection, Tree-sitter parse/highlight, decoration publication, disabled/invalid fallback, docs/registry, and API inventory checks pass.
    - Performance: No-hot-path source guards, parse cancellation/stale rejection, payload/cache budget tests, and syntax smoke remain within typed budgets or documented advisory limits.
    - Code Quality: `cargo fmt --check`, focused tests, docs registry checks, primitive docs checks, Rust visibility checks, and package docs checks pass.
    - Security: First-party-only artifact loading, package-root path confinement, malformed query/artifact rejection, no language-specific Rust branches, and no authority expansion tests pass.
  - Approach:
    - Documentation Reviewed:
      - `docs/wiki/modules/maintenance-validation.md`.
    - Options Considered:
      - Run only new tests: fast, but misses API/docs/visibility regressions.
      - Run focused implementation + docs/API/security gates: appropriate for new public primitive.
    - Chosen Approach:
      - Run focused suites and document known pre-existing unrelated failures separately if full workspace tests still have them.
    - API Notes and Examples:
      ```bash
      cargo fmt --check
      CARGO_TARGET_DIR=target/pi-verify cargo test --test syntax_grammar --quiet
      CARGO_TARGET_DIR=target/pi-verify cargo test --test package_primitive_gate --quiet
      CARGO_TARGET_DIR=target/pi-verify cargo test --test parse_coordinator --quiet
      CARGO_TARGET_DIR=target/pi-verify cargo test --test decoration_transport --quiet
      CARGO_TARGET_DIR=target/pi-verify cargo test --test editor_performance_invariants --quiet
      CARGO_TARGET_DIR=target/pi-verify cargo test --test package_loading_docs --quiet
      CARGO_TARGET_DIR=target/pi-verify cargo test --test clay_js_api_inventory --quiet
      CARGO_TARGET_DIR=target/pi-verify cargo test --test clay_js_doc_registry --quiet
      CARGO_TARGET_DIR=target/pi-verify cargo test --test rust_visibility_api_mapping --quiet
      CARGO_TARGET_DIR=target/pi-verify cargo test --test primitives_docs --quiet
      cargo run -- smoke-gui --config-fixture syntax-grammars
      ```
    - Files to Create/Edit:
      - `plans/038-Phase18.10-Package-Provided-Tree-sitter-Grammar-and-Syntax-Highlighting-Primitive.md`: update task checkboxes and completion evidence during execution.
      - `docs/development/launch-and-gui-smoke.md`: manual smoke instructions/result notes.
    - References:
      - `.agents/skills/project-patterns/references/maintenance-validation.md`
  - Test Cases to Write:
    - Manual smoke: with syntax fixture loaded, open `.rs`, `.ts`, `.js`, verify visible highlights, type/edit/scroll, confirm responsive local editing, disable/omit a package and verify editable no-highlight fallback.
  - Completion Evidence:
    - Added runnable manual syntax smoke fixture directory `tests/fixtures/configuration/syntax-grammars/init.js` containing only the end-user `loadPackage("@clay/rust")`, `loadPackage("@clay/typescript")`, and `loadPackage("@clay/javascript")` calls; this makes the documented command `cargo run -- smoke-gui --config-fixture syntax-grammars` resolve through the CLI fixture loader.
    - Updated `docs/development/launch-and-gui-smoke.md` so Phase 18.10 manual syntax smoke uses the runnable fixture command, names the Rust/TypeScript/JavaScript fixture files, and records the deterministic automated coverage that stands in for visual GUI inspection in headless agent runs.
    - Added `tests/manual_smoke_docs.rs::phase18_10_manual_syntax_smoke_has_runnable_fixture_contract` to pin the smoke command, fixture, fixture files, editability/fallback expectations, and no raw-op/manual-registration setup.
    - Added `tests/syntax_grammar.rs::manual_syntax_smoke_contract_is_covered_by_deterministic_fixture_flow`, which loads all three first-party grammar packages into `SyntaxGrammarRegistry`, selects active syntax grammars for `.rs`, `.ts`, and `.js` paths while preserving `core.code`, parses each fixture before and after a small edit through `TreeSitterSyntaxHandler`, verifies syntax decorations, and verifies unloaded fallback stays in `core.code` with no active syntax grammar.
    - Attempted the GUI smoke command in this agent: `timeout 90s env CARGO_TARGET_DIR=target/pi-verify cargo run -- smoke-gui --config-fixture syntax-grammars` compiled, launched the managed local server, connected the client, and created a window before the timeout terminated the process; visual file-opening/highlight inspection is not possible through this non-interactive harness, so deterministic smoke coverage above verifies the same contract artifacts.
    - Verification: `cargo fmt --check` clean; `git diff --check` clean; `CARGO_TARGET_DIR=target/pi-verify cargo test --test syntax_grammar --test package_primitive_gate --test parse_coordinator --test decoration_transport --test editor_performance_invariants --test package_loading_docs --test clay_js_api_inventory --test clay_js_doc_registry --test rust_visibility_api_mapping --test primitives_docs --test manual_smoke_docs --test package_loading --quiet` passed (338 tests across 12 suites); `node --check` passed for all three grammar package load entries.

- [x] Update or verify the code wiki after implementation
  - Acceptance Criteria:
    - Functional: The project code wiki is updated after all implementation tasks are complete, or explicitly verified as unchanged for non-code work.
    - Performance: Wiki updates add no runtime work and document performance-relevant implementation details changed by the plan: background Tree-sitter parsing, viewport-bounded decoration publication, cache/payload budgets, and no-hot-path policy.
    - Code Quality: Wiki pages explain what changed code does, how it works, invariants/tradeoffs, source/test paths, examples where useful, and links from the master wiki index.
    - Security: Wiki pages document touched security boundaries, first-party-only grammar artifacts, package-root path confinement, permissions, validation, and no authority expansion without exposing secrets.
  - Approach:
    - Documentation Reviewed:
      - `.agents/skills/project-wiki/SKILL.md`: Use the project wiki workflow and quality bar.
    - Options Considered:
      - Update after each task: more granular, but noisy and likely to churn.
      - Update once after tests pass: keeps docs aligned with final code.
    - Chosen Approach:
      - After implementation and verification pass, update the Markdown code wiki once using `project-wiki`, including the master index and relevant pages.
    - API Notes and Examples:
      ```text
      docs/wiki/index.md
      docs/wiki/modules/syntax-grammar-contributions.md
      docs/wiki/modules/parse-coordinator.md
      docs/wiki/modules/decoration-transport.md
      docs/wiki/modules/mode-registry.md
      ```
    - Files to Create/Edit:
      - `docs/wiki/index.md`: Add or update navigation links for changed implementation areas.
      - `docs/wiki/modules/syntax-grammar-contributions.md` (new, tentative): Tree-sitter grammar contribution internals.
      - `docs/wiki/modules/parse-coordinator.md`: Update syntax-provider integration.
      - `docs/wiki/modules/decoration-transport.md`: Update syntax decoration flow/style-token mapping.
      - `docs/wiki/modules/mode-registry.md`: Update active syntax grammar vs active major mode.
      - `docs/wiki/modules/package-loading.md`: Update grammar-only package loading if implementation changes loading behavior.
      - `docs/wiki/modules/phase18.10-tree-sitter-grammar-primitive-review.md`: Ensure review page reflects final implementation.
    - References:
      - `.agents/skills/project-wiki/SKILL.md`
  - Test Cases to Write:
    - Manual wiki review: Confirm the master index links relevant pages and updated pages explain what changed implementation does and how it works.
  - Completion Evidence:
    - Updated `docs/wiki/index.md` descriptions for Syntax Grammar Registry, Package Loading, Mode Registry, Decoration Transport, and Parse Coordinator so the master index points to the final Phase 18.10 implementation areas and mentions `clay:syntax`, active syntax selection, Tree-sitter handler flow, and manual-smoke fixture coverage.
    - Updated `docs/wiki/modules/syntax-grammar-registry.md` with final implementation details for `runtime/js/syntax.ts`, `src/server/ops/syntax.rs`, `clay:syntax.serverRegisterSyntaxGrammar`, `loadPackage` syntax grammar summaries, first-party grammar package load entries, the runnable `tests/fixtures/configuration/syntax-grammars/init.js` smoke fixture, and deterministic manual-smoke coverage.
    - Updated `docs/wiki/modules/parse-coordinator.md` and `docs/wiki/modules/decoration-transport.md` to document Tree-sitter syntax work flowing through existing `ParseCoordinator`, bounded `ParseWindowSnapshot` inputs, `TreeSitterSyntaxHandler`, stale/payload-budget validation, `DecorationSet` publication, `DECORATION_PAYLOAD_BUDGET_BYTES`, and `SyntaxChunkCache`.
    - Updated `docs/wiki/modules/package-loading.md`, `docs/wiki/modules/mode-registry.md`, `docs/wiki/modules/clay-js-facade-skeleton.md`, and `docs/wiki/modules/phase18.10-tree-sitter-grammar-primitive-review.md` to remove stale planned/future wording and document final `clay:syntax`, `clay.contributions.syntaxGrammars`, active syntax grammar versus major-mode separation, final implementation status, source paths, tests, hot-path constraints, and security boundaries.
    - Added `tests/package_loading_docs.rs::phase18_10_code_wiki_documents_final_syntax_implementation` to keep the master wiki index, relevant wiki pages, final source/test paths, performance/security terms, smoke fixture, and absence of stale planned-unavailable wording synchronized with the implementation.
    - Verification: `cargo fmt --check` clean; `git diff --check` clean; `CARGO_TARGET_DIR=target/pi-verify cargo test --test package_loading_docs --test primitives_docs --test manual_smoke_docs --test syntax_grammar --quiet` passed (147 tests across 4 suites); stale wording scan returned 0 matches for old Phase 18.10 future/stub wording in docs/wiki, docs/development, packages, runtime, and src.

## Compromises Made
- To be filled after tasks are completed and tests pass.

## Further Actions
- To be filled after task completion with improvements, rationale, and priority.
