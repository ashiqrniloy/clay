# Plan: Phase 18.16 — Tiered Tree-sitter Syntax Engine

Implements `roadmap.md` Phase 18.16 and decision `decision-logs/2026-07-09-0352-tiered-tree-sitter-themable-syntax-vocabulary-theme-registry-and-opt-in-lsp.md` component 1. This supersedes Plan 045's web-tree-sitter-only engine choice while preserving its binding non-blocking open-parse and `ParseCoordinator::finish_task` diagnostic fixes.

## Objectives

- Stand up one engine-agnostic grammar-to-`DecorationSpan` pipeline that emits Phase 18.15 vocabulary tokens and modifiers rendered through `StyleRegistry`.
- Add three syntax backends: Tier 1 native first-party `tree-sitter-*` crates, Tier 2 host-side `web-tree-sitter` WebAssembly adapter, and Tier 3 package-JS parser fallback.
- Make engine selection deterministic, provenance-recorded, and user/package-initiated: first-party defaults to Tier 1; higher-priority Tier 2 override and user forced tier are explicit.
- Fix open-time parse so text renders immediately and handler errors surface as `RuntimeDiagnostic`s.
- Ship real Rust/TypeScript/JavaScript/Markdown query/grammar artifacts or documented reproducible build steps with provenance.

## Expected Outcome

- Rust, TypeScript, JavaScript, and Markdown highlight in production via Tier 1 native tree-sitter and emit vocabulary tokens/theme modifiers.
- Tier 2 `web-tree-sitter` can override a first-party grammar or serve a package grammar through the same pipeline; Tier 3 JS parser remains available for grammar-less languages.
- Open file render is not blocked by parse completion; parse failures produce visible runtime diagnostics rather than silent hangs.
- No per-language Rust branches exist in server/client/editor hot paths; language names live in data registrations only.
- Linux validation passes: `cargo fmt --check`, `cargo check --all-targets`, `cargo clippy --all-targets -- -D warnings`, and focused syntax/parse/package/docs tests.

## Tasks

- [ ] Entry gate: verify Phase 18.15 and native grammar compatibility
  - Acceptance Criteria:
    - Functional: Phase 18.15 vocabulary/theme contract is complete and tests prove `DecorationSpan` has `token_type`/`modifiers` and rendering uses `StyleRegistry`; candidate native grammar crates build as runtime dependencies on Linux with host `tree-sitter`.
    - Performance: Native grammar crate check does not introduce runtime parse work yet; dependency impact is recorded before implementation.
    - Code Quality: Exact crate versions and APIs are verified with Cargo-local docs before coding against Tree-sitter APIs.
    - Security: No new package authority is granted; third-party grammar loading remains first-party-only unless later decision approves trust/integrity.
  - Approach:
    - Documentation Reviewed:
      - `roadmap.md` Phase 18.16.
      - `decision-logs/2026-07-09-0352-tiered-tree-sitter-themable-syntax-vocabulary-theme-registry-and-opt-in-lsp.md`.
      - `docs/reference/primitives/syntax-vocabulary.md`.
      - Context7: `npx ctx7@latest library "Tree-sitter" "Create plan document for Phase 18.16: Tiered Tree-sitter Syntax Engine (Native First-Party + Web-tree-sitter + JS Fallback): native tree-sitter Rust crates, web-tree-sitter wasm adapter, incremental parsing, query captures, version compatibility, highlighting"` selected `/websites/rs_tree-sitter` and `/tree-sitter/tree-sitter`.
      - Context7: `npx ctx7@latest docs /websites/rs_tree-sitter "Phase 18.16 tiered syntax engine: Rust tree-sitter Parser, Language, Query, QueryCursor, WasmStore/web-tree-sitter equivalents, incremental parse old_tree, timeout, grammar crate version compatibility, highlights.scm capture iteration"` documented `Parser`, `Query::new`, `QueryCursor::new`, `ts_parser_parse(old_tree)`, and Wasm grammar initialization.
    - Options Considered:
      - Skip local crate verification because dev-dependencies already exist: rejected; Phase 18.16 promotes native grammars to production dependencies.
      - Verify locally first: chosen; exact crate versions matter more than generic examples.
    - Chosen Approach:
      - Run `cargo metadata --format-version 1`, `cargo tree -i tree-sitter`, `cargo tree -i tree-sitter-rust`, `cargo tree -i tree-sitter-typescript`, `cargo tree -i tree-sitter-javascript`, and add/check Markdown grammar candidate. Generate exact rustdoc with `CARGO_TARGET_DIR=target/pi-docs cargo doc -p tree-sitter --no-deps` plus grammar crates where possible.
    - API Notes and Examples:
      ```bash
      cargo metadata --format-version 1
      cargo tree -i tree-sitter
      CARGO_TARGET_DIR=target/pi-docs cargo doc -p tree-sitter --no-deps
      ```
    - Files to Create/Edit:
      - `Cargo.toml`, `Cargo.lock`: move selected first-party grammar crates from dev-only to runtime dependencies and add Markdown grammar crate if selected.
      - `plans/047-Phase18.16-Tiered-Tree-sitter-Syntax-Engine.md`: record exact versions/check result when executing.
    - References:
      - `.agents/skills/project-patterns/references/language-capability-sequencing.md`.
      - `.agents/skills/project-patterns/references/protocol-and-performance.md`.
  - Test Cases to Write:
    - `native_tree_sitter_crates_are_runtime_compatible`: asserts crate versions and language symbols are available under normal build, not only tests.

- [ ] Review existing syntax primitives and plan generic tier gaps before implementation
  - Acceptance Criteria:
    - Functional: A primitive-review wiki page inventories current grammar registry, parse coordinator, decoration transport, vocabulary/theme registry, package loading, JS parse bridge, and package grammar metadata; it identifies only generic tier-engine gaps.
    - Performance: Review locks no-hot-path parsing/query compilation/package JS in keypress, paint, layout, scroll, pointer, or text-event handlers.
    - Code Quality: Review rejects per-language Rust branches, hard-coded grammar-name switches, raw renderer callbacks, and package-specific client behavior.
    - Security: Review preserves first-party-only resolver authority for grammar artifacts and rejects runtime downloads, shell/package-manager execution, raw ops, native handles, and client JS.
  - Approach:
    - Documentation Reviewed:
      - `docs/reference/primitives/index.md`, `registry.md`, `parse-update-strategy.md`, `rendering-strategy.md`, `package-security.md`, `syntax-vocabulary.md`.
      - `docs/wiki/modules/primitive-architecture.md`, `parse-coordinator.md`, `decoration-transport.md`, `syntax-grammar-registry.md`, `phase18.10-tree-sitter-grammar-primitive-review.md`.
      - `.agents/skills/project-patterns/references/mode-primitive-first.md` and `language-capability-sequencing.md`.
    - Options Considered:
      - Patch `TreeSitterSyntaxHandler` directly: rejected; would miss Tier 2/Tier 3 selection and docs/security invariants.
      - Inventory primitives first, then implement minimal generic gaps: chosen.
    - Chosen Approach:
      - Create a Phase 18.16 primitive review that states reused primitives, gaps (`SyntaxEngineTier`, engine selection/provenance, native grammar registry entries, web adapter bridge, vocabulary style-map migration, open-parse diagnostics), rejected approaches, and tests.
    - API Notes and Examples:
      ```text
      SyntaxGrammarContribution -> SyntaxEngineSelection -> ParseCoordinator -> DecorationSet(TokenType, Modifiers)
      ```
    - Files to Create/Edit:
      - `docs/wiki/modules/phase18.16-tiered-tree-sitter-engine-primitive-review.md`: new primitive review.
      - `docs/wiki/modules/primitive-architecture.md`, `docs/wiki/index.md`: link review.
      - `tests/primitives_docs.rs`: guard review/index markers.
    - References:
      - `.agents/skills/create-plan/references/clay.md` primitive-first and package-provided grammar requirements.
  - Test Cases to Write:
    - `phase18_16_tiered_engine_primitive_review_linked_and_complete`: checks wiki/index links, reused primitives, gaps, rejected approaches, hot-path and security markers.

- [ ] Implement engine-neutral grammar-to-vocabulary pipeline and open-parse diagnostics
  - Acceptance Criteria:
    - Functional: One pipeline loads a resolved grammar/query/style map, parses bounded windows incrementally when available, extracts query captures, maps captures to `TokenType` + `Modifiers`, rejects unmapped captures, rejects stale versions, cancels superseded work, and publishes bounded `DecorationSet`s. `ParseCoordinator::finish_task` turns handler errors/timeouts into sanitized `RuntimeDiagnostic`s.
    - Performance: Query compilation and grammar initialization occur at registration/load time or cached first use, not per paint/key; parse work stays `Background`, viewport-bounded, cancellable, and within `INCREMENTAL_PARSE_UPDATE_BUDGET_BYTES`, `DECORATION_PAYLOAD_BUDGET_BYTES`, and `SYNTAX_CACHE_BUDGET_BYTES`.
    - Code Quality: Existing `TreeSitterSyntaxHandler` shape is reused where possible; pipeline takes data-driven grammar descriptors and has no language-name `match`/`if` branches.
    - Security: Diagnostics sanitize paths/details; parser output remains inert spans with package/core provenance; no raw callbacks or raw ops cross the boundary.
  - Approach:
    - Documentation Reviewed:
      - `docs/wiki/modules/parse-coordinator.md`, `docs/wiki/modules/syntax-grammar-registry.md`, `docs/reference/primitives/syntax-vocabulary.md`.
      - Context7 Tree-sitter docs: `Parser`, `Query::new`, `QueryCursor`, old-tree parse API.
    - Options Considered:
      - Keep old `styleMap` mapping to free-form `style_token`: rejected; Phase 18.15 made vocabulary the rendering contract.
      - Map captures directly to `TokenType`/`Modifiers` in one shared path: chosen.
    - Chosen Approach:
      - Introduce a small engine result trait/struct that returns capture ranges + capture names; feed it through one style-map-to-vocabulary mapper and existing decoration validation. Extend `finish_task` to publish `RuntimeDiagnostic` for handler errors without blocking open.
    - API Notes and Examples:
      ```rust
      // Shape, not final API:
      SyntaxCapture { byte_range, capture_name }
      capture_mapper.map("function.declaration") -> (TokenType::Function, Modifiers::DECLARATION)
      ```
    - Files to Create/Edit:
      - `src/server/syntax.rs`: shared grammar pipeline, vocabulary mapper, native handler reuse.
      - `src/server/parse_coordinator.rs`: handler-error diagnostic publication.
      - `src/server/connection.rs`: non-blocking open parse follow-up path.
      - `src/protocol/decorations.rs`: helper mapping only if needed.
      - `tests/syntax_grammar.rs`, `tests/parse_coordinator.rs`, `tests/selected_file_*_smoke.rs`: focused coverage.
    - References:
      - `decision-logs/2026-07-08-2316-web-tree-sitter-host-adapter-for-syntax-highlighting-and-non-blocking-open-parse.md` binding carry-forward fixes.
  - Test Cases to Write:
    - `syntax_pipeline_maps_captures_to_vocabulary_tokens`.
    - `open_document_renders_before_background_parse_completes`.
    - `finish_task_publishes_runtime_diagnostic_for_handler_error`.
    - `syntax_pipeline_rejects_stale_or_oversized_decoration_output`.

- [ ] Add Tier 1 native first-party grammar registration
  - Acceptance Criteria:
    - Functional: Rust, TypeScript, TSX, JavaScript, JSX/MJS/CJS, and Markdown native grammars register at startup as first-party syntax engine entries using data descriptors; default selection uses Tier 1 for first-party languages.
    - Performance: Parser/query instances are cached per grammar/contribution; repeated open/edit does not recreate parsers per parse; incremental old-tree reuse is used where safe.
    - Code Quality: No per-language Rust control-flow branches exist; language-specific facts live in registration data (`language_id`, extensions, grammar function, query path, style map).
    - Security: Compiled-in grammar data is first-party Clay-maintained; no arbitrary native/third-party artifact loading.
  - Approach:
    - Documentation Reviewed:
      - `src/server/syntax.rs`, `tests/syntax_grammar.rs` current test-only `TreeSitterSyntaxHandler` coverage.
      - `Cargo.toml` current `tree-sitter` and grammar crate dependencies.
    - Options Considered:
      - Keep first-party packages as wasm-only and skip native: rejected by decision 0352.
      - Register native grammars as package-like first-party descriptors at startup: chosen; generic engine, data-driven dispatch.
    - Chosen Approach:
      - Add a startup native grammar registry seeded from first-party package metadata and static grammar language functions. Keep package provenance in diagnostics as `@clay/<language>` or core-owned first-party equivalent.
    - API Notes and Examples:
      ```rust
      NativeGrammarDescriptor {
          package: "@clay/rust",
          language_id: "rust",
          extensions: &["rs"],
          tier: SyntaxEngineTier::Native,
      }
      ```
    - Files to Create/Edit:
      - `src/server/syntax.rs`: native descriptor/registry and selection integration.
      - `src/server/mod.rs` or server startup owner: seed native descriptors.
      - `Cargo.toml`, `Cargo.lock`: runtime grammar dependencies.
      - `tests/syntax_grammar.rs`, `tests/editor_performance_invariants.rs`: native selection/no-branch guards.
    - References:
      - `.agents/skills/project-patterns/references/language-capability-sequencing.md`.
  - Test Cases to Write:
    - `tier1_native_first_party_is_default_for_known_extensions`.
    - `native_parser_instance_is_cached_per_grammar`.
    - `tiered_engine_has_no_language_specific_rust_branches`.

- [ ] Add Tier 2 web-tree-sitter host adapter and artifact contract
  - Acceptance Criteria:
    - Functional: A generic host-side adapter in the existing Deno/V8 background worker loads resolver-validated package `grammars/*.wasm` and `queries/highlights.scm`, runs query captures, and returns the same capture/vocabulary pipeline output as Tier 1. A higher-priority Tier 2 package can override Tier 1 only when user/package selection permits it.
    - Performance: `web-tree-sitter` runtime and grammar wasm are bundled/local; no network fetch; wasm initialization/query compile is cached; work runs only in background parse lane.
    - Code Quality: Adapter is shared host code, not copied per package; no per-language JS wrappers except package metadata/load calls.
    - Security: Wasm/query paths are package-root-confined, first-party-only for this phase, with provenance recorded; no shell/package-manager/runtime download/native-library authority.
  - Approach:
    - Documentation Reviewed:
      - Plan 045 sections for generic host `web-tree-sitter` adapter and artifact vendoring.
      - Context7 library lookup for `web-tree-sitter` returned Tree-sitter docs but no separate package-specific result within the three-command budget; implementation must verify npm package docs/source before coding.
      - `docs/reference/packages/creating-packages.md` grammar-only contract.
    - Options Considered:
      - Per-package JS wrapper around `web-tree-sitter`: rejected; duplicates engine plumbing.
      - One host adapter invoked by package metadata: chosen.
    - Chosen Approach:
      - Vendor/package the `web-tree-sitter` runtime in a host-owned path, expose an internal runtime module that package load can delegate to, and make Rust see only the existing parse handler/capture result boundary.
    - API Notes and Examples:
      ```js
      // Shape, not final public API:
      await registerTreeSitterGrammarHandler({ grammar: "./grammars/rust.wasm", query: "./queries/highlights.scm" });
      ```
    - Files to Create/Edit:
      - `runtime/js/syntax.ts` or successor: generic web-tree-sitter adapter module.
      - `src/server/js_runtime.rs`: bundle/allow host adapter module and runtime assets.
      - `packages/*/grammars/*.wasm`: Tier 2 artifacts where shipped.
      - `tests/js_runtime*`, `tests/syntax_grammar.rs`: adapter loading and override tests.
    - References:
      - `plans/045-Native-Tree-sitter-Syntax-Highlighting-Non-blocking-Open-Parse-and-Markdown-Folding.md` superseded plan, useful artifact/adapter notes.
  - Test Cases to Write:
    - `web_tree_sitter_runtime_is_bundled_and_loadable_without_network`.
    - `tier2_wasm_override_suppresses_tier1_when_user_selected`.
    - `tier2_rejects_grammar_path_outside_package_root`.

- [ ] Retain Tier 3 JS parser fallback and implement explicit engine selection/configuration
  - Acceptance Criteria:
    - Functional: Existing package-JS parse handlers keep working for grammar-less languages; engine selection order is default Tier 1 for first-party, explicit higher-priority Tier 2 override when authorized, explicit user forced tier via `init.js`, else Tier 3 fallback for packages without grammar.
    - Performance: Selection is package-load/open/reclassification work only; no selection recomputation in paint/key/text hot paths.
    - Code Quality: Forced-tier config is a documented Clay JS API, not an ad hoc JSON/TOML key; selection diagnostics explain chosen tier and provenance.
    - Security: Package cannot silently self-promote over native; override is user-initiated or package-declared with explicit user load/authorization and provenance.
  - Approach:
    - Documentation Reviewed:
      - `docs/reference/primitives/package-loading.md`, `docs/reference/clay-js-api/configuration.md`, `docs/reference/clay-js-api/syntax/server-register-syntax-grammar.md`.
      - `.agents/skills/project-patterns/references/configuration-system.md`.
    - Options Considered:
      - Silent package override by priority only: rejected; decision 0352 requires user-initiated engine selection.
      - Clay JS configuration API such as `clay.syntax.setSyntaxEnginePreference(...)`: chosen if implementation needs user tier forcing.
    - Chosen Approach:
      - Add a small engine preference model with per-language/package keys and docs; keep default behavior zero-config and one-line `loadPackage` compatible.
    - API Notes and Examples:
      ```js
      import { setSyntaxEnginePreference } from "clay:syntax";
      setSyntaxEnginePreference("rust", "wasm"); // explicit override; exact API may change during API task
      ```
    - Files to Create/Edit:
      - `src/server/syntax.rs`: engine selection/preference state.
      - `src/server/ops/syntax.rs`, `runtime/js/syntax.ts`: config facade/op if needed.
      - `docs/reference/clay-js-api/syntax/*`: engine preference docs if public.
      - `tests/syntax_grammar.rs`, `tests/configuration*`: selection coverage.
    - References:
      - `.agents/skills/create-plan/references/clay.md` package default loading and configuration requirements.
  - Test Cases to Write:
    - `js_parser_fallback_still_runs_without_tree_sitter_grammar`.
    - `user_forced_tier_is_honored_and_recorded_in_provenance`.
    - `package_cannot_silently_override_native_tier`.

- [ ] Ship first-party queries/grammar artifacts and syntax smoke coverage
  - Acceptance Criteria:
    - Functional: Rust/TypeScript/JavaScript/Markdown packages have real `queries/highlights.scm` and Tier 2 `grammars/*.wasm` artifacts or a documented reproducible build/provenance path; grammar directories no longer contain only placeholder README files for shipped engines.
    - Performance: Fixtures prove bounded decoration payloads and non-blocking open for each first-party language.
    - Code Quality: Style maps produce Phase 18.15 vocabulary tokens/modifiers, not legacy free-form-only `style_token` families.
    - Security: Artifact provenance records upstream source/release/size/integrity; no untrusted third-party grammars.
  - Approach:
    - Documentation Reviewed:
      - `packages/{rust,typescript,javascript,markdown}/package.json`, `queries/highlights.scm`, `grammars/README.md`.
      - `docs/reference/packages/{rust,typescript,javascript}.md` and package authoring guide.
    - Options Considered:
      - Commit wasm artifacts: simple runtime, larger repo.
      - Document build step only: smaller repo, more setup risk.
      - Choose per artifact based on reproducibility and size; record provenance either way.
    - Chosen Approach:
      - Prefer committed first-party artifacts when reasonably sized; otherwise add deterministic build commands and tests that fail with clear setup messages.
    - API Notes and Examples:
      ```text
      packages/rust/grammars/rust.wasm
      packages/rust/queries/highlights.scm
      ```
    - Files to Create/Edit:
      - `packages/rust/**`, `packages/typescript/**`, `packages/javascript/**`, `packages/markdown/**`: grammar/query/style-map/provenance updates.
      - `docs/reference/packages/*.md`: update engine tier docs.
      - `tests/fixtures/syntax/*`: per-language smoke fixtures.
      - `docs/development/launch-and-gui-smoke.md`: manual GUI syntax smoke steps.
    - References:
      - `.agents/skills/project-patterns/references/language-capability-sequencing.md` package-provided grammar tests.
  - Test Cases to Write:
    - `first_party_language_fixtures_produce_themed_vocabulary_decorations`.
    - `first_party_artifact_provenance_is_recorded`.
    - Manual smoke: `cargo run -- smoke-gui`, open `.rs`, `.ts`, `.tsx`, `.js`, `.md`, confirm text renders before highlights and highlights arrive.

- [ ] Create or verify Clay configuration APIs
  - Acceptance Criteria:
    - Functional: Any syntax-engine preference, package override, or default-load behavior introduced by this phase is exposed through documented Clay JS configuration APIs using `~/.config/clay/init.js`; if no new user-facing config is needed beyond existing `loadPackage`, document that explicitly.
    - Performance: Configuration is evaluated at startup/package-load/reclassification, never on keypress/paint.
    - Code Quality: No hidden syntax-engine keys; docs include examples, defaults, custom properties, key binding metadata, permissions, and lookup tags.
    - Security: Config does not grant filesystem, network, shell, package-manager, native-library, AI, client-JS, or arbitrary third-party grammar authority.
  - Approach:
    - Documentation Reviewed:
      - `.agents/skills/create-plan/references/clay.md` Clay Configuration Task.
      - `.agents/skills/project-patterns/references/configuration-system.md`.
      - `docs/reference/clay-js-api/configuration.md`.
    - Options Considered:
      - Add config only if forced tier is implemented: minimal.
      - Pre-create broad syntax settings: rejected as speculative.
    - Chosen Approach:
      - Document or implement the smallest API needed for explicit engine selection; otherwise verify existing `loadPackage` remains the only required user setup.
    - API Notes and Examples:
      ```js
      import { loadPackage } from "clay:packages";
      await loadPackage("@clay/markdown");
      ```
    - Files to Create/Edit:
      - `docs/reference/clay-js-api/configuration.md` and `docs/reference/clay-js-api/syntax/*.md` as needed.
      - `docs/index.md`, `docs/generated/clay-js-api-registry.json`, `docs/reference/clay-js-api/api-inventory.toml` if public API docs change.
      - Runtime facade/op files if a new config API is implemented.
    - References:
      - `decision-logs/2026-05-08-1841-configuration-through-init-js-and-clay-js-apis.md`.
  - Test Cases to Write:
    - `syntax_engine_configuration_is_documented_or_explicitly_not_needed`.
    - `syntax_engine_preference_requires_documented_clay_js_api` if new config exists.

- [ ] Create or verify Clay JS APIs for public programmatic surfaces
  - Acceptance Criteria:
    - Functional: Public syntax-engine, grammar-registration, parse-handler, and diagnostic/config surfaces introduced or changed by this phase have stable Clay JS facade APIs and Markdown docs; internal Rust helpers are private or `pub(crate)`.
    - Performance: Public APIs do not let user JS run in typing/rendering hot paths.
    - Code Quality: Docs include stable ID, user-facing name, key bindings or empty list, custom properties, examples, errors, permissions/security notes, backing Rust path, op wrapper, facade path, and lookup tags.
    - Security: No raw `Deno.core.ops`, arbitrary Rust public functions, executable callbacks, raw CSS/colors, native handles, or broad grammar loading are exposed.
  - Approach:
    - Documentation Reviewed:
      - `.agents/skills/create-plan/references/clay.md` Clay JS API Task.
      - `.agents/skills/project-patterns/references/clay-js-api-boundary.md`, `clay-js-api-naming.md`, `clay-js-api-schema.md`, `documentation-as-code.md`, `doc-registry-tests.md`.
      - `docs/reference/clay-js-api/syntax/server-register-syntax-grammar.md`.
    - Options Considered:
      - Reuse `serverRegisterSyntaxGrammar` only: likely enough for grammar metadata.
      - Add a new syntax-engine preference API: only if forced-tier config requires it.
    - Chosen Approach:
      - Inventory changed public Rust functions after implementation, expose only actual public programmatic needs, and update docs/registry in the same task.
    - API Notes and Examples:
      ```bash
      cargo test rust_visibility_api_mapping
      cargo test clay_js_api
      ```
    - Files to Create/Edit:
      - `src/server/ops/syntax.rs`, `runtime/js/syntax.ts`: facade/op changes if needed.
      - `docs/reference/clay-js-api/syntax/*.md`, `docs/reference/clay-js-api/api-inventory.toml`, `docs/generated/clay-js-api-registry.json`, `docs/index.md`.
      - `tests/rust_visibility_api_mapping.rs`, API/docs registry tests.
    - References:
      - `decision-logs/2026-05-08-1509-clay-js-api-facade-for-rust-functions.md`.
  - Test Cases to Write:
    - `phase18_16_public_rust_surfaces_have_js_api_or_are_crate_private`.
    - `syntax_engine_api_docs_registry_are_fresh`.

- [ ] Run focused and full verification
  - Acceptance Criteria:
    - Functional: All tier behavior, fallbacks, diagnostics, docs, and package loading tests pass.
    - Performance: Source guards prove no parser/package JS/IPC work in paint/text-event handlers; budget tests prove parse windows and decoration payloads remain bounded.
    - Code Quality: Linux blocking checks pass: fmt, check all-targets, clippy all-targets with `-D warnings`, and relevant tests.
    - Security: Tests cover path confinement, first-party-only artifact loading, missing permissions, silent override rejection, and sanitized diagnostics.
  - Approach:
    - Documentation Reviewed:
      - `.agents/skills/project-patterns/references/maintenance-validation.md`.
      - `.agents/skills/project-patterns/references/protocol-and-performance.md`.
    - Options Considered:
      - Run only focused tests: rejected for phase completion.
      - Focused tests during tasks, full Linux gate at end: chosen.
    - Chosen Approach:
      - Run focused suites after each implementation step, then full Linux gate before marking phase complete.
    - API Notes and Examples:
      ```bash
      cargo fmt --check
      cargo check --all-targets
      cargo clippy --all-targets -- -D warnings
      cargo test --all-targets
      ```
    - Files to Create/Edit:
      - Test files only as needed to close gaps found during verification.
    - References:
      - `AGENTS.md` platform-validation instructions.
  - Test Cases to Write:
    - Source guards: no `tree_sitter`, package parser, JS runtime, or IPC references in Masonry paint/text-event paths.
    - Manual smoke docs check: first-party language open/highlight/fallback workflow documented.

- [ ] Update package-author, primitive, and development docs
  - Acceptance Criteria:
    - Functional: Public/reference docs describe Tier 1/Tier 2/Tier 3 engine selection, grammar/query/style-map vocabulary mapping, artifact provenance, non-blocking open parse, handler diagnostics, user override rules, and JS fallback.
    - Performance: Docs state budget/hot-path constraints and representative commands.
    - Code Quality: Master docs index links all new/changed docs; docs tests fail if links or required tier markers are missing.
    - Security: Docs state first-party-only artifact loading, path confinement, no runtime downloads/shell/package-manager/native handles, and third-party trust deferral to Phase 23.
  - Approach:
    - Documentation Reviewed:
      - `docs/reference/packages/creating-packages.md`, `docs/reference/primitives/parse-update-strategy.md`, `docs/reference/primitives/rendering-strategy.md`, `docs/reference/primitives/package-security.md`, `docs/development/launch-and-gui-smoke.md`.
    - Options Considered:
      - Document only implementation internals in wiki: rejected; package authors need public contract.
      - Update both public package docs and internal wiki: chosen.
    - Chosen Approach:
      - Update public docs before final wiki task; keep wiki as implementation guide linking to canonical public docs.
    - API Notes and Examples:
      ```text
      Tier 1 native first-party -> Tier 2 web-tree-sitter override -> Tier 3 JS fallback
      ```
    - Files to Create/Edit:
      - `docs/reference/packages/creating-packages.md`.
      - `docs/reference/primitives/parse-update-strategy.md`, `rendering-strategy.md`, `package-security.md`, `registry.md`, `index.md`.
      - `docs/reference/packages/{rust,typescript,javascript,markdown}.md`.
      - `docs/development/launch-and-gui-smoke.md`, `docs/index.md`.
      - `tests/primitives_docs.rs`, docs/manual smoke tests.
    - References:
      - `.agents/skills/create-plan/references/clay.md` Package-Provided Grammar Task.
  - Test Cases to Write:
    - `tiered_syntax_engine_docs_are_indexed_and_security_boundaries_recorded`.
    - `package_author_guide_documents_native_wasm_js_fallback_routes`.

- [ ] Update or verify the code wiki after implementation
  - Acceptance Criteria:
    - Functional: The project code wiki is updated after all implementation tasks are complete, or explicitly verified as unchanged for non-code work.
    - Performance: Wiki updates add no runtime work and document performance-relevant implementation details changed by the plan.
    - Code Quality: Wiki pages explain what changed code does, how it works, invariants/tradeoffs, source/test paths, examples where useful, and links from the master wiki index.
    - Security: Wiki pages document touched security boundaries, permissions, validation, secrets handling, or external authority without exposing secrets.
  - Approach:
    - Documentation Reviewed:
      - `.agents/skills/project-wiki/SKILL.md`: Use the project wiki workflow and quality bar.
    - Options Considered:
      - Update after each task: more granular, but noisy and likely to churn.
      - Update once after tests pass: keeps docs aligned with final code.
    - Chosen Approach:
      - After implementation and verification pass, update the Markdown code wiki once using `project-wiki`, including the master index and relevant pages.
    - API Notes and Examples:
      ```text
      docs/wiki/index.md
      docs/wiki/modules/<module>.md
      ```
    - Files to Create/Edit:
      - `docs/wiki/index.md`: Add or update navigation links for changed implementation areas.
      - `docs/wiki/modules/syntax-grammar-registry.md`: Tiered engine selection/native/web/JS notes.
      - `docs/wiki/modules/parse-coordinator.md`: non-blocking open and diagnostic publication details.
      - `docs/wiki/modules/decoration-transport.md`: vocabulary-token output invariants if changed.
      - `docs/wiki/modules/phase18.16-tiered-tree-sitter-engine-primitive-review.md`: final implementation notes if needed.
    - References:
      - `.agents/skills/project-wiki/SKILL.md`.
  - Test Cases to Write:
    - Manual wiki review: Confirm the master index links relevant pages and updated pages explain what changed implementation does and how it works.

## Compromises Made

- To be filled after tasks are completed and tests pass.

## Further Actions

- To be filled after task completion with improvements, rationale, and priority.
