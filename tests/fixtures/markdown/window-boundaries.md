Guard text before the visible range.

```js
const visible = "fenced window";
```

# Window Hé 🦀

Paragraph with **strong**, *emphasis*, and `inline code`.

- bullet at chunk edge
1. ordered at chunk edge
