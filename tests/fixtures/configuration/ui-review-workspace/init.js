// Plan 118 review fixture: one open markdown document in the workspace view.
// The rail reads the document's `## HH:MM — title` headings, so the fixture
// carries real outline content instead of an empty document.
