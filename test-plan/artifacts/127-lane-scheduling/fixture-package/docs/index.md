# @fixture/lane

Plan 127 manual-test fixture package. Registers one slow parse handler for the
`lane` mode (third-party general lane) and one module-backed completion provider
(third-party latency lane). Not a distributable package.
