# Clay UI review capture

- Fixture: ui-review-completion (default Control Centre binding)
- Logical window: 900×600
- Screenshot: screenshot.png
- Accessibility dump: accessibility.txt

This run uses a private mode-700 temporary HOME/config/data/socket root and
fixture-only documents. It never reads the ambient Clay configuration.

Interactive step: press the default `Ctrl+X Ctrl+P` chord to capture the
visible centered Command Centre. This live X11 run used the private completion
fixture root and the loaded built-in/package command catalogue.
