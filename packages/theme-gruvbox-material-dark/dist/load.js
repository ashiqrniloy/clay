// Inert load entry for @clay/theme-gruvbox-material-dark.
// Theme overrides are static `clay.contributions.textStyles` in package.json;
// nothing registers at runtime. This no-op keeps the package-load contract.
export {};