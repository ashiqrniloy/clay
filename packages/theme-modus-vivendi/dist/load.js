// Inert load entry for @clay/theme-modus-vivendi.
// Theme overrides are static `clay.contributions.textStyles` and `clay.contributions.designTokens` in package.json;
// nothing registers at runtime. This no-op keeps the package-load contract.
export {};
