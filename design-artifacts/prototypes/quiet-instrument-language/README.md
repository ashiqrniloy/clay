# prototypes · quiet-instrument-language (rejected alternative)

Language **A** of the 2026-09-11 design proposal: the "restrained neobrutal"
style rebuilt with better hierarchy, denser spacing and a real focus ring.

- `workspace-neobrutal.html` — Workspace screen, language A
- `agent-neobrutal.html` — Coding Agent screen, language A
- `ds-neobrutal.css` — language A: 0px radii, 2px structural borders, hard
  offset shadows, opaque fills, 100ms snappy transitions
- `theme.css`, `ds.js`, `workspace-data.js` — the shared kit, copied so this
  folder stays openable standalone

**Not approved.** The user adopted language B (Quiet Instrument) instead — see
`design-artifacts/approved/quiet-instrument-language/` and `DESIGN.md`. Kept for
history: this folder has no authority over implementation.

Open any HTML file directly over `file://`; the theme switcher in the header
cycles every palette in `theme.css`.
